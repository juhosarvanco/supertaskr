# State

Updated: 2026-08-14 by planner, claude-fable-5 @chat-session

## Just completed
Convention frozen at v0.1.3 (method/) after best-practice research:
EARS criteria, touches + parallelism guardrails, verifier security
sweep, suggestion channel with single-writer architect, room
resolution ritual, succession guarantee. Market map and steal list
compiled (docs/research/competitors.md). Vision, dashboard design,
and naming rounds banked (docs/). Project restructured to eat its
own dog food — this folder now IS an nputer project. Audit pass filed
the last chat-only content (docs/design/principles.md); the folder is
now the complete record.

## Interview
COMPLETE (all 7): first user = technical builder · success criteria
in NORTH_STAR · non-goals + constraints in NORTH_STAR · stack ADR-007
· riskiest assumption = plan-keeps-agents-coherent (leading indicators
in NORTH_STAR) · first slice = milestone 1 "the mirror".

## In progress / broken right now
Q1 RESOLVED (rooms/first-user.md): first user = the technical builder
with an agent CLI and a project outgrowing their control; non-coders
later via the paid spec-studio layer. Interview now at Q2 (success
criteria). No tasks exist yet by design:
decomposition runs only after the interview completes. Name DECIDED: nputer (rooms/naming.md resolved) — npm/PyPI free;
domain + trademark sweep remains.

## Next up (1–3)
1. Dispatch T-001 to an executor session (method/roles/executor.md,
   own worktree). T-002 may run in parallel — touches are disjoint.
Decomposition DONE: docs/tasks/ T-001…T-007 populated with EARS
   criteria, sizes, touches, dependencies. Board frontier: T-001
   (app shell) and T-002 (parser) are unblocked and parallel-safe
   (disjoint touches).
2. Verify T-001/T-002 per verifier role; integrate; first checkpoint.
3. Domain (.dev/.fi/.com) + trademark sweep for "nputer".

## Open questions
None — milestone 1 is dispatchable.
