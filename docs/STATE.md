# State

Updated: 2026-09-02 at the fourth Fable checkpoint — the newest file in
docs/checkpoints/ is **the fourth Fable sitting record** (thirteen
merges, two red mains of the seat's own making, the guard's silent
allows named); the first three sit beside it. **Lanes are live (derive:
LANES).** **CI IS A SEPARATE CLAIM FROM A LOCAL BATTERY AND MUST BE
READ**: `gh run list` before believing the tree.

**NOTHING IS BROKEN.** Designed non-zero: `npm run health` **3** while
bands await keepers (T-156-s1/s2) — never read it as clean, never "fix"
it. **AN EXIT MAY MEAN THE GATE NEVER RAN**: `docs-gate.mjs`'s
`CANNOT_RUN: 3` sits in a catch inside `main()`. **READ THE OUTPUT, NOT
THE CODE**: a verdict prints gate lines, a crash a stack trace. **Re-run
a suspect ONCE, then ATTRIBUTE by NAME at the base.**

## The contract this file is under

REPLACED at every checkpoint from docs/STATE-template.md, AFTER the
record is written, in the SAME commit (ADR-019) — and any LATER edit to
that record re-touches this file in the same commit, or the docs gate
reads STATE as STALE for every path. STATE keeps the MECHANISM; the
INSTANCE is stamped in the record. A figure appears here only with its
derive command. **When the byte band warns, content MOVES to the record
— a hazard is never deleted to fit.** **The commit subject opens with
`Checkpoint:`** (T-182).

## Live right now — derive, never quote

- LANES: `git worktree list --porcelain | awk '/^branch refs\/heads\/task\//'`
  — a detached entry is NOT a lane. **The dispatch ritual and its order
  are orchestrator 5b/5c's** (T-239 builds the arm). Never read the
  ledger's FREE column as a verdict (T-143).
- **STAMP BEFORE YOU CUT; CUT, ARM, THEN CUT THE NEXT; READ EVERY STAMP
  BACK**: a stamp is a no-op on a key the card lacks and a perl pattern
  ending in `\s*$` glues two lines (rooms 13–19). **DERIVE `brief.mjs
  --dispatch` BEFORE THE STAMP**: a card stamped past a shared fence
  cost a lane cut and a revert (the fourth record).
- **FENCE BY PATH, AND ASK THE GUARD**: `brief.mjs --write-fence`
  refuses an overlap; remove a merged lane's worktree before arming its
  successor. **A widening is the seat's, both halves** (touches on main,
  the lane's card copy by hand — room 16), and **the blind verifier's
  phase 1 is the earliest reader of the fence**: act on its tree facts
  at once (room 23).
- THE HUMAN'S APP: **1420 is CONVENTIONS' PORT RULE**. `../nputer-app` is
  detached ON PURPOSE: not a lane.
- BOARD CENSUS: `brief.mjs --state`; the parser's field is `blockedBy`.
- **E2E PORT AND SCRATCH FILENAMES ARE CONVENTIONS' RULES** — derived
  per lane, never defaulted (T-217).
- GRAPH: `cargo run -p nputer-index -- index --check --root ../..` from
  app/src-tauri/ — ASK IT after every write. **A regen moves six dogfood
  pins in app/test that no lane can see: run architecture-dogfood and
  map-dogfood-render BEFORE the merge commit** (room 27). Never trust it
  from a drill worktree (T-153-s3).

## Next up — hooks only; statuses are the board's

<KEEP THIS HEADING NAMED "Next up": brief.spec.ts pins it.>

1. IN FLIGHT: **DERIVE IT** — `brief.mjs --dispatch`. A hand-kept list
   here named two dead lanes and missed two live ones (T-142).
2. **THIRTEEN LANDED THIS STRETCH** (T-205 blindness-as-spawn, T-228,
   T-225-s2, T-214, T-112-s6 among them). Live at the record: T-216-s8,
   T-202-s1, T-239, T-205-s8, T-225-s12. Queue by fence: T-238-s1 (p2,
   behind T-216-s8), T-120-s2 (ALONE on tools/e2e), T-205-s2/T-225-s10
   (app-dispatch), T-215-s2, T-216-s6/T-228-s2 (CONVENTIONS, behind
   T-239). **Order and reasoning: the fourth record.**
3. **TRIAGE IS OWED AT THE STAMP** (orchestrator 2): `--dispatch --full`
   is the TRIAGE view (T-225; T-225-s12 brings it under the buffer).
4. **ASK THE DOCS GATE WHAT A CHANGE OWES** — `docs-gate.mjs <paths>`;
   it does NOT read placement fields or ID SHAPE — the parser's smoke
   test does (T-235): `T-NNN-sN` only, one level.
5. **T-126-s2 IS RULED**; **NO BLOCKER IS NAMED HERE**: `blocked_by` IS
   READ FROM THE CARD (T-138).
6. **@human holds; no card is cut from these** — the FORM, the STEERING
   SPLIT (T-180), T-025-s4, T-162-s1, T-131, the interview's narrow-width
   ending, T-229-s3's runner cost, the stray f.txt/g.txt, and
   docs/rooms/loop-efficiency.md (27 items, open, @human's).

## Standing hazards — the section that saves the hour

- **A COMMAND HERE CARRIES ITS CWD AND ITS ARGUMENT.** `npm run
  boot:check` runs FROM `tools/e2e/`; `npm run health -- --readings
  <FILE>` needs the `--` and the RUNNER'S OWN CAPTURE (`gate-run` prints
  its path) — in zsh pass the flags as an ARRAY, never one string.
- **SPELL THE PUSH BARE: `git -C <checkout> push origin main`**, nothing
  before, nothing after. A `cd <dir>;` before it and a redirection after
  it each make an arm of the guard ALLOW UNJUDGED with a notice a
  PreToolUse allow never shows (T-216-s8 in flight, T-216-s9). **THE
  GUARD READS CI (T-237)**: a run in flight refuses a push unless
  `NPUTER_CANCEL_CI=<id>`; a red newest verdict is announced.
- **THREE WRITES THAT RED THE TREE AND NO CHEAP GATE SEES**: a test
  rename owes `npm run capabilities` (in the merge commit); any .ts
  moved — a pin fix included — owes the graph regen; a prose commit
  stales the push token. **EVERY PUSH OWES THE FOUR-SUITE BATTERY, RUN
  LAST** (T-203): ~10 min warm, ~20 under five lanes.
- **THE SOLO LOCK COLLIDES BETWEEN A BENCH AND ITS LANE for every
  eight-character card id** until T-202-s1 lands (room 26): a refusal
  naming a pid you did not start is that, not a foreign checkout.
- **A BENCH OLDER THAN A SIBLING LANE REDS session-economics** by ref
  skew (T-205-s8 in flight): attribute at the base, never to the diff.
- **A SEAT'S OWN SHELL IS A HAZARD**: `cd` persists, `set -e` does not
  stop a failing heredoc, zsh spells `pipestatus`, a grep for "Tests"
  matches the FAILED separator — key every check on an EXIT CODE and
  read the diff back (room 12, the fourth record).
- **A RELAYED FACT IS A CLAIM**: say whose; never relay the attack set.
  **A PHASE-2 DISPATCH WAITS FOR THE EXECUTOR'S REPORT, AND A BENCH
  STANDS UNTIL THE VERIFIER'S** (rooms 21–22).
- **MOVE THE LANE BRANCH TO THE VERDICT COMMIT BEFORE MERGING**
  (`git branch -f task/<lane> <sha>`; room 17) and take the LANE'S card
  copy on a conflict. **Build app/ after merging app sources, BEFORE the
  battery** (room 18).
- **ONE HOLDER OF THE INTEGRATION CHECKOUT, ON DISK (T-238)**:
  `.nputer/holder.json`, `brief.mjs --take-seat` / `--release-seat`.
- **POISON DRILLS**: kill-set containment, the site the property lives,
  a DATA mutant where the property is data; a mutant that lands and dies
  at a self-check measures nothing (verifier.md 2b, T-223-s5).
- **BOOT GATE AND HEALTH BANDS ARE OWED AT EVERY CHECKPOINT** (T-046,
  T-156); when the triage band drifts past 9 net arrivals a checkpoint
  is DUE, not after the wave. **A lane's token meter exists ONLY in its
  notification.**

## The records

- docs/checkpoints/ — append-only; the fourth Fable sitting record is
  the newest. Pre-compaction: 2026-08-27-backfill-STATE.md.
- docs/rooms/governing-docs.md + ADR-019 — this file's contract.
- Every earlier version: `git log -- docs/STATE.md`.
