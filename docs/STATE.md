# State

Updated: 2026-09-15 at the rulings-day-and-the-split checkpoint by the
architect seat — the newest file in docs/checkpoints/ is **that record**
(eight merges; method 0.1.34; the session boundary).
**LANES ARE LIVE ONLY IF DERIVED (derive: LANES).** **CI IS A SEPARATE CLAIM FROM A LOCAL BATTERY AND
MUST BE READ**: `gh run list` before believing the tree — **main is
GREEN on the runner at 811c4bd7; read every run after it.**
**NOTHING IS BROKEN LOCALLY.** Designed non-zero: `npm run health` **3**
while bands await keepers (T-156-s1, T-262) — never read it as clean,
never "fix" it; triage/live-suggestions BREACHES (T-306). **`--merge` MAY STOP FALSELY (T-295-s7/s8/s4; a census on a seat line): rule through, finish by hand, diff corrected files against the bench tip.** **AN EXIT MAY MEAN THE GATE NEVER RAN**:
`docs-gate.mjs`'s `CANNOT_RUN: 3` sits in a catch inside `main()`.
**READ THE OUTPUT, NOT THE CODE.** **Re-run a suspect ONCE, then
ATTRIBUTE by NAME.**

## The contract this file is under

REPLACED at every checkpoint from docs/STATE-template.md, AFTER the
record is written, in the SAME commit (ADR-019); an APPEND to that
record does not (T-143-s5). STATE keeps the MECHANISM; the INSTANCE is
in the record. A figure appears here only with its derive command.
**When the byte band warns, content MOVES to the record — a hazard is
never deleted to fit.** The commit subject opens with `Checkpoint:`.
**THE OTHER HALF OF THE STANDING READ IS docs/INDEX.md** (ADR-024
decision 2; generated by `npm run capabilities`, never summarised).

## Live right now — derive, never quote

- LANES: `git worktree list --porcelain | awk '/^branch refs\/heads\/task\//'`
  — a detached entry is NOT a lane. **Dispatch is the arm**:
  `brief.mjs --dispatch-lane <id> --slug <slug> --executor <m@k>
  --verifier <m@k> --scratch <dir>`. **DERIVE `brief.mjs
  --dispatch --full` BEFORE THE STAMP.**
- **THE MERGE IS THE ARM** (T-295, ADR-024 decision 3): `brief.mjs
  --merge <id> [--bump <old>..<new>] [--meters <report>] [--tier <t>]`
  performs the ritual and STOPS staged — the seat rules, commits, runs
  `gate-run.mjs --owed-set --range`, then the bare push. The readings
  land in docs/checkpoints/meters.jsonl. **BUILD
  lib/parser FROM main BEFORE THE VERB, FROM THE MERGED TREE AT ITS
  STOP** (T-317). **THE WAIT IS THE ARM** (T-298): `brief.mjs
  --await <marker>|--await-pid <pid> --ceiling <s>`.
- **THE BENCH IS TWO SPAWNS** (5d): phase 1 pasted the card at base +
  the condensed role, its return saved as `attack-set-<id>.md` and
  hashed with ground truths taken AT THE BASE; phase 2 a FRESH spawn on
  `../supertaskr-V-<id>`, `git checkout --quiet --detach <tip>`; it
  runs the range form of the owed set (ADR-024 decision 4).
  **A BENCH STANDS UNTIL THE VERIFIER'S LAST NOTIFICATION.** **THE ARM
  RENDERS BOTH PHASES (T-296), NO VERIFIER
  PACK (T-296-s10), NO MEASUREMENTS ON THE STANDARD GROUND
  (T-296-s11)**: append the seat's addendum, RESEAL by hand.
- **THE ASK FILE IS THE ONLY CHANNEL**: a lane writes
  `<scratch>/ask-<id>.md`, the seat answers in the same file; a
  widening is the seat's, both halves (room 16); **THE WATCH LIST IS
  DERIVED FROM THE LIVE LANES and fires on a CHANGED file too**; a
  grant that lands after the stamp is applied by a fresh executor
  spawn, never by the seat's hand (T-295). **A ROOM OR DECISION ENTRY IS PROPOSED VERBATIM AND
  APPENDED ON THE OWNER'S YES; it paraphrases and dates, never quotes**
  (orchestrator.md 8b).
- THE HUMAN'S APP holds **1420** (the boot gate ABORTS while it does);
  `../supertaskr-app` is detached ON PURPOSE.
- BOARD CENSUS: `brief.mjs --state`; the parser's field is `blockedBy`.
- **E2E PORT AND SCRATCH FILENAMES ARE CONVENTIONS' RULES** (T-217):
  lane 15<card>, bench 25<card>, `<purpose>-<card>.<ext>`.
- GRAPH: `cargo run -p supertaskr-index -- index --check --root ../..` from
  app/src-tauri/ — ASK IT after every write, **and again after every
  integrator correction at a merge**; a regen moves six dogfood pins in
  app/test (room 27).
- THE SEAT: `.supertaskr/holder.json` (T-238);
  `brief.mjs --take-seat` / `--release-seat`.

## Next up — hooks only; statuses are the board's

<KEEP THIS HEADING NAMED "Next up": brief.spec.ts pins it.>

1. **DERIVE IT** — `brief.mjs --dispatch --full`.
2. **NO LANE IS LIVE — THE SESSION BOUNDARY** (ruled 2026-09-14): a new
   seat starts from this file, docs/INDEX.md and
   supertaskr-evidence/handoff-2026-09-14/ — first the T-320
   demonstration on main, T-204's re-measure, then the T-312 rerun;
   lanes in parallel where fences are disjoint; T-301 dormant (a screen
   is DESIGNED WITH THE OWNER before any build).
3. **WITH THE OWNER**: the three check-evidence-reuse cards; the T-301
   design sitting; INDEXED_DOCS and the topic files; T-266 wakes
   2026-10-01.
4. **A LANE RUNS ITS SUITES ONCE** (T-279), scoped with `--owning`
   (T-271); the push, the bench and CI owe the set their own range owes
   (`--range <base>..<tip>`; the guard re-derives it; T-280, T-294).
5. **ASK THE DOCS GATE WHAT A CHANGE OWES** — `docs-gate.mjs <paths>`;
   a token for a file under a tracked directory is a NEW-FILE
   RESERVATION (T-287); a directory with nothing under it stays DEAD.
6. **The owner holds; no card is cut from these** — the ROADMAP heading
   and ARCHITECTURE front-door labels, section fences (G), charter
   entry 32, T-173, the domains and the license (at the first cut).

## Standing hazards — the section that saves the hour

- **NEVER SPELL THE PRE-RENAME IDENTIFIER IN LIVING TEXT** — the
  keeper scans comments and workflow files.
- **A COMMAND HERE CARRIES ITS CWD AND ITS ARGUMENT** (`npm run
  boot:check` FROM `tools/e2e/`; `npm run health -- --readings <FILE>`
  with the `--`; in zsh pass flags as an ARRAY, never one string).
- **SPELL THE PUSH BARE: `git -C <checkout> push origin main`**, nothing
  before, nothing after; the PreToolUse guard pre-judges any command
  carrying a push.
  **KEEP THE LANE BRANCH UNTIL CI IS GREEN** (the landing arm reads it).
- **A LOCAL GREEN IS NOT A RUNNER GREEN**: the runner has no git
  identity and no login (T-239-s4) and its disk has a floor (T-278);
  read the run log, attribute by name, file the card.
- **THREE WRITES THAT RED THE TREE AND NO CHEAP GATE SEES**: a test
  rename owes `npm run capabilities` (in the merge commit; it also
  regenerates docs/INDEX.md); any .ts moved owes the graph regen; a
  prose commit stales the push token. **EVERY PUSH OWES ITS RANGE'S OWED
  SET, RUN LAST** (T-280) — **a COMMIT or a staged merge during the run
  UNKEYS the token**: hold every write until it finishes. **GATE THE
  MERGE COMMIT ON THE COUNTS** (2d6d354).
- **A SUBAGENT THAT "FINISHES" WHILE ITS OWN JOB RUNS RE-FIRES**: never
  spawn a continuation into a live lane. **A `<-` IN CARD PROSE IS A PROVENANCE ARROW** to the
  preflight: respell it in words; an absolute path in prose is exit 3;
  **a criterion names only a path the fence carries**; **a filed card
  carries its notes and Verdicts sections from birth**; **a dated ###
  under Verdicts that is not a verdict reads as the newest** (T-311-s10).
- **A RELAYED FACT IS A CLAIM**: say whose; a `review:` message relays
  the Codex orchestrator — ask the owner's own yes. **A FENCE OVERLAPPING
  A LIVE LANE PREFLIGHTS AT EXIT 1**; the fence WRITER refuses it even
  for a generated file (regenerate on the bench, at the merge).
- **POISON DRILLS**: kill-set containment, the site the property lives,
  a DATA mutant where the property is data (verifier.md 2b).
- **BENCH RUNS USE PORT 25<card>**; **AN AMENDMENT UNDER ITS OWN HEADING
  IS INVISIBLE TO THE CRITERIA READERS**; **THE CENSUS READS AN ORDINAL
  WORD IN CARD PROSE AS UNSTAMPED**; **A MUTANT BLOCK THAT ADDS CODE OR
  IS A SUBSTRING OF ITS FIX STOPS THE VERB** — apply by hand, drill,
  e2e whole at the merged tree.
- **BOOT GATE AND HEALTH BANDS ARE OWED AT EVERY CHECKPOINT** (T-046);
  the whole four suites run at every checkpoint and nightly
  (ADR-024 decision 4).

## The records

- docs/checkpoints/ — append-only; the newest is the 2026-09-15
  rulings-day-and-the-split record; meters.jsonl is the bands' readings.
- docs/decisions/025 — one session contract, either harness (T-311..T-316).
- docs/rooms/governing-docs.md + ADR-019 — this file's contract.
- Every earlier version: `git log -- docs/STATE.md`.
