# Checkpoint: T-153 (2026-08-29, integrator nputer-4e, direct pipeline)

## Merge

Parents `47979ee` (main, at the T-158 checkpoint) and the lane tip
only, `--no-ff`. Forecast `merge-tree --write-tree` exit 0; the
merge's diff (at-merge pair): **4 paths** — `docs_watch.rs`, the
card, and the two suggestion cards the lane filed (`T-153-s1`,
`T-153-s2`), which reach the board with this merge. Disjoint from
the live `T-154` lane and the triage branch (whose base predates
these cards) by construction.

## The integrator's owed corrections, per the verdict

The verifier ruled three figure defects live in the LANE'S NOTES and
owed correction here before any checkpoint quotes them: the
conversion census is **twenty call sites** (notes said seventeen)
across **eight loop bodies** (notes said nine) plus seven bare waits
correctly outside the class; and the notes' range row was stamped at
an earlier lane ref — at the merged tip the executor-pair diff was 4
paths. Quoted corrected, per the parent rule: defects in a RECORD
are corrected by the seat that owes the record, with the original
left standing on the card.

## Gates

- GRAPH REGEN: **OWED** (1 of 4 paths is `.rs`) and deferred by the
  lane to this checkpoint, correctly (three lanes were live at its
  build). Regenerated here: 3 lines of `loc`, byte count UNCHANGED at
  1,020,023 — the identical-figures trap's benign twin — and
  `index --check` CURRENT after, 98.1%, 19,977 left. App suite re-run
  over the regenerated graph: 1013/1013 (no fixture counts moved, as
  the lane predicted).
- BOOT GATE: `app/src-tauri/**` matched — the lane ran it at build
  (`NPUTER_BOOT_PORT=14522`, both `[nputer]` lines, exit 0); not
  re-run here, the merge adds nothing the lane's tip lacked.
- DOCS GATE: FIRES on the three card paths; all three named suites
  run green below.

GRAPH, asked LAST: after this record's final write —
`graph.json is CURRENT (1020023 bytes, 189 files, 2152 symbols,
2111 edges)`, exit 0.

## Suites (counts and exits, unpiped)

cargo 518 passed / 0 failed / 4 ignored, exit 0 · parser 314/314
exit 0 · app 1013/1013 exit 0 (twice: before and after the regen) ·
e2e 233/233 exit 0 (port 16017, lsof zero rows first) · lint:docs 0 ·
lint:tokens 0.

## CI

The card's red is closed on Linux: the failing body reads 198/0 on
ubuntu across four runs at four lane refs (verified read-only at
verification). CI's cargo step still reds on `T-153-s2`'s E2BIG —
the argv defect in C-14's fence, deterministic, now a board card via
this merge — so the push-hold on main STANDS until s2 lands; then
main pushes, CI runs green end to end for the first time, and draft
PR #1 closes. The four steps behind cargo still await first Linux
contact behind s2.

## Environment

One lane live after this checkpoint (`T-154`, verdict pending) plus
the triage worktree on its own branch. T-153's worktree REMOVED after
this commit, branch kept. Port 1420 untouched; `z` still not ours.

## What the brief got wrong

Nothing material; the lane corrected two operational details in
flight and the verifier corrected the lane's three figures — every
correction is a record entry, none was code.

## Metrics (ADR-020)

Rework cycles: **0** (approved first pass). Tokens, STAMPED from the
usage meters: executor 292,209 + verifier 191,333 ≈ **484k** for the
arc — notably ~40% cheaper than T-158's rejected-then-fixed arc,
which is the first datapoint the metrics slot exists to make cheap.
Gate runtime: not derivable here (T-157 pending).

## Dispositions

Stamped: T-153 done / same-model, both hands recorded. Arriving with
the merge: `T-153-s1` (suggested — the graph writer's own one-emit
claim, same class one file over) and `T-153-s2` (suggested — the
E2BIG fixture), the latter promoted and dispatched by the integrator
immediately after this checkpoint at @human's standing authorization,
because it is the last blocker before CI can run green.
