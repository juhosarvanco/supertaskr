# Checkpoint: T-025-s6 (2026-08-30, integrator nputer-4e, blind-verified)

The real smoke has a VERDICT — four premises judged by variant and
never by content, the recorder printed whole before any judgement,
and a `cliNotFound` names itself in milliseconds instead of a
fifteen-minute timeout. @human's genesis walk can no longer be
misreported by a green exit over a dead run.

## The ceremony that earned its seat

The triage refence (bare path -> `app-agent` slug) moved this diff
onto the verifier-owed row; the executor took the heavier reading and
stopped at `verifying`; a BLIND verifier was paid — and the blindness
paid back: its attack set, written from the contract alone, produced
two mutants the executor's seven missed, and BOTH SURVIVED. (1) The
function converting the judgement into the exit status could be
gutted to `let _ = missing;` with the suite green at zero warnings.
(2) The find-not-last independence claim was a comment no test held.
Verdict: APPROVED WITH ASSIGNED CORRECTIONS — both performed at this
merge, committed-first, and PROVEN by re-planting the verifier's own
survivors: each now dies at its new pin (V4 red at the both-direction
verdict pin; V3 red at "a trailing event after the terminal must not
unmake the turn"). The verifier also disclosed its blindness was
procedural, not structural — the dispatch carried executor
shape-claims below the marker in the same message; future verifier
dispatches spawn factless and receive the facts by follow-up message
after the attack set is committed.

## Suites (exits unpiped, after merge + corrections)

parser 315/315 · app 1015/1015 · e2e **320 passed** exit 0 · cargo
exit 0 (the corrected body among the passes) · graph REGENERATED and
CURRENT — **the alarm reads 4,693 left** (this lane +2,172, the
corrections' 35 lines the rest): T-135 Half B -> T-140-s1 is now the
board's most urgent chain, about four files of floor remaining.

## Metrics (ADR-020)

Rework: 0 in-lane; 2 verdict corrections at merge. Tokens: executor
208,945 (98 uses, 27 min) + verifier 155,842 (55 uses, 17 min).
Cold start: NONE. Drift incidents: NONE. Lanes after: NONE —
the second wave is fully landed. T-025-s7 (the ceremony-row policy
question) queues suggested for the next sitting. Port 1420 untouched.
