# Checkpoint: T-127-s6 (2026-08-29, integrator nputer-4e, direct pipeline)

`arch cycles` answers **ACYCLIC, exit 0, on main** — for the first
time in the map's history. The registry's last declared cycle
`C-08 -> C-09 -> C-08` is broken for real: no import severed, no file
moved, three files re-owned into two new components (C-17 board-model,
C-18 board-root), and the verifier's C3 drill proved it from the side
nobody had — the NEW expectations red under the OLD truth (6f/1007).
STATE's standing "one command exits 1 on purpose" headline retires at
this checkpoint.

## Merge

Parents `4717ad1` (main) and lane tip `7468689`, `--no-ff` ->
`37045cd`. 14 paths: the registry (C-05/08/09/12/13 edited, C-17/C-18
created), both dogfood fixtures, crate-index's allowlist (emptied,
enforcement kept and drilled from BOTH sides), the card, three
suggestion cards (s7 the slug-block fourth copy, s8 the census-blind
reader class, s9 the lint-vs-parser YAML gap). Verdict: **APPROVED
WITH FIVE ASSIGNED CORRECTIONS**, all performed here:

- AC1/AC2/AC3 — three narrative attributions corrected to the
  verifier's per-edge derivation (one of six edges was Board->drawer,
  not all six; Board.tsx was one of five files, single only after the
  model left; the "ten sites" are six model imports — one a test
  file — plus four former C-09 imports).
- AC4 — the card's CONTROL figure stamped `@ f0ff62d` (856 at tip).
- AC5 — T-127-s7's slug-block line landed in this same merge
  (`app-board -> C-08, C-09, C-11, C-17, C-18`), PLUS the third stale
  sentence s7 missed ("three multi-component slugs" -> two, `app-map`
  never qualified).

Also performed, routed by the verdict as out-of-fence stale claims:
CONVENTIONS' two cycle-held-open sentences retired with the date, and
its "git remote returns ZERO remotes" claim retired — the first push
happened 2026-08-29 and the CI step IS the enforcing copy now.
`workflow-parity.spec.ts:342`'s stale comment stays for the tools/e2e
queue (that fence is held by T-153-s5's lane).

## Gates

- GRAPH REGEN: OWED and performed — regenerated, CURRENT at 1,021,562
  bytes (98.2%, 18,438 left; only loc/hashes moved, totals identical —
  the no-import-severed proof in the graph's own terms).
- capabilities: REGENERATED — 21,992 bytes CURRENT (was stale by one
  behaviour from the integrator's own direct spec-body fix at 852c6f2;
  T-153-s8's instance, discharged here).
- DOCS GATE exit 0 · lint:tokens exit 0 · BOOT GATE: not owed by this
  seat's derivation (the lane ran it: exit 0).

GRAPH, asked LAST: after this record's final write — CURRENT,
1,021,562 bytes, exit 0.

## Suites (counts and exits, unpiped)

cargo **522/0/4** exit 0 · parser 314/314 exit 0 · app **1013/1013**
exit 0 · e2e **281/281** exit 0 (`NPUTER_E2E_PORT=14680`; the one red
the lane predicted — brief.spec.ts:439 — cleared by AC5 in this same
commit) · `arch cycles` **exit 0**.

## Seat notes for the eval corpus

- The verifier reached the crate-doc-comment judgement independently
  in phase 1 (corroboration, not copy) and disclosed one accidental
  out-of-permission write of its own (a generator run without
  `--check`), reverted and sha256-proved before the verdict — the
  disclosure discipline holding on both sides of the fence hook's
  blind spot (Bash-mediated writes; T-154-s2's standing class).
- Two seats, two drills, disjoint mutant sets: sixteen
  assertion-side (executor) + registry-side/two-sided (verifier). The
  one defect class that survived both was narrative attribution — a
  comment restating a measurement in prose — which is exactly the
  class the corrections fixed and MF-06's corpus exists to catch.

## Metrics (ADR-020)

Rework cycles: **0**. Tokens: executor 317,757 + verifier 310,250 ≈
**628k** for the pair; with the predecessor's honest stop (163,599),
the whole cycle-removal arc ≈ **792k**. Lanes after this checkpoint:
T-153-s5 (verifier running). T-127-s6's worktree REMOVED, branch
kept. Port 1420 untouched; `z` unmoved. Main NOT pushed — the push
still waits behind T-153-s5/s6 for the green-run attempt.
