# Checkpoint: T-093 (2026-08-27, architect claude-opus-5 as integrator)

## Merge

`91413c8`, parents `bc2d82a` and `db36301`, `--no-ff`. Forecast
`git merge-tree --write-tree bc2d82a task/T-093-lane`, **exit 0 read
from `$?` before the substitution**; the merge's own range
`bc2d82a..91413c8` gives the same **4 paths**: `docs/CONVENTIONS.md`
and three `T-093*` cards. Nothing outside the fence
`[docs/CONVENTIONS.md]`. **Only lane live, so disjointness was
vacuous** — stated rather than claimed as a check that ran.

## Gates

- **GRAPH REGEN — NOT OWED and asked anyway.** `index --check` exit 0
  before and after; the four paths are markdown.
- **BOOT GATE — NOT OWED**, 0 of 4 under `app/src-tauri/**`,
  `app/src/**` or either manifest.
- **DOCS GATE — FIRES**, exit 1 on the path set, naming the four
  suites below; all four run.
- **ADR-019 BUDGET GATE — HOLDS.** `docs/CONVENTIONS.md` **86,718 →
  95,644 (+8,926)** against warn **107,967**, fail **129,560**;
  12,323 bytes of headroom remain. `lint:docs` exit 0.

## Suites

parser **314/314** exit 0 · app **1013/1013** exit 0 · e2e **233/233**
exit 0 (port 16041, `lsof` zero rows before the bind) · `lint:docs` 0 ·
`lint:tokens` 0 · `index --check` 0. Cargo not re-run at the merge: the
range holds no `.rs` and no gate owes it — stated rather than skipped.

## Board

`T-093` stamped `done` in the lane by its own executor per the ceremony
row (S, outside shipped code, no verifier). Two suggestions arrive with
it: `T-093-s1`, `T-093-s2`. `T-092` remains `planned` and is the next
dispatch; it holds the same `docs/CONVENTIONS.md` token, so the two were
never concurrently dispatchable.

## Environment

Read 2026-08-27 on Mac.lan: port 1420 held by `node` pid 19746 on
`[::1]:1420`, the human's app in `/Users/ujju/Projects/nputer-app` —
`lsof` only, never connected, never bound. Lane port 16041 probed at
zero rows immediately before binding. The untracked zero-byte `z` at the
repo root predates this session and was left alone.

## What the brief got wrong

**The dispatch instruction was wrong, for the second identical time.**
The ceremony row for an S card outside shipped code makes the executor
its own integrator; my brief said *"Do NOT work on main"* and carried no
row-11 merge instruction. The lane correctly refused to guess upward
against an explicit restriction and reported instead.

**This is the same error I made on `T-145` six hours earlier**, and
`T-150`'s executor had already reported the cause between the two:
**row 11 — the ceremony row, the status to stamp, whether to merge —
lives in `method/tasks/TASK-FORMAT.md`, which the root adapter's
read-first set does not name.** Twice reported, twice repeated. Filed
as `T-152`.

Four further corrections the lane made to its own card, all verified by
it at the base ref: the second cause misattributes `grep -c`'s behaviour
to the wrong tool; the third cause's needle **no longer reproduces**
because ADR-019's compaction reflowed the line; `T-090` carries the same
stale needle and is append-only so was named not edited; and the card's
"all five files removed in this commit" was already discharged — acting
on it would have breached the fence.

**And a third standing rule was found to live only in briefs.** `git
grep -i charset` over `method/`, the four governing documents and
`docs/decisions/` returns **zero**, positive control eight hits over
`docs/`. After `pkill` and `git update-ref`, the `file --mime` rule is
the third prohibition in two days that existed only in the dispatching
session's memory. CONVENTIONS is now its only standing copy.

## Dispositions

`T-093` → `done`, `review: self-verified`. `T-093-s1` filed (the new
prose has no mechanical reader — proved by a mutant that inverted the
headline and left every keeper green). `T-093-s2` filed, recommending
**T-142 absorbs T-093's residue one-way**, arms 1 and 2 only, arm 3
left as its own `tools/e2e` build. Worktree removed and pruned.
