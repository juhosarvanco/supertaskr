# Checkpoint: T-143-s4 (2026-08-30, integrator nputer-4e, direct pipeline)

The in-flight row assertion measures each card's OWN BLOCK instead of
a 600-character constant — the CI-only red that broke the streak at
run #376 is fixed at its cause, and THE PUSH RESUMES AT THIS
CHECKPOINT after being held through two landings.

## Merge and evidence

Parents `35e6504` and lane tip `a5b8913`, `--no-ff`; closing stamps
follow. 3 paths: the spec (+93), the card, suggestion `T-143-s5`.
The lane's proof was unusually complete and each piece verified here:
the structural window (five assertions per card, including the
exactly-one-row guard that stops a structural window becoming a
bigger net, and the opposite-arm absence); the clone measurement
BOTH WAYS (pre-fix red truncating mid-word at `4d3dd8f`; post-fix 13
passed — and `T-163` measured 49 characters from the same cliff,
which is how close the next red was); a FIVE-arm drill all red one
side each, restorations sha256-proved, with the planting failure
DISCLOSED (perl interpolation planted nothing twice; re-run through
an exact-string planter that refuses unless the needle occurs once);
and the merge tree against moved main materialized, object-verified
(`git write-tree` answered the same object) and run GREEN at 320
before the merge was made. `scripts/dispatch-order.mjs` untouched,
sha256 unmoved — only the spec's ruler changed.

## Integrator's correction at this checkpoint, committed with it

The T-164 filing (@human's launcher card) declares `touches: [bin]`,
a creation target in T-160's class-(a) language — and the live-board
census (lib/parser fence.test.ts) requires every fence token to
resolve, T-054's three excepted. The census now carries the fourth
exception WITH its reason and its leaving condition (the day bin/
exists). A filing of this seat's redded a suite one merge later; the
exception list is the house shape for declared-but-not-yet-real.

## T-143-s5, filed by the lane, instance already discharged

STATE went stale against its own record TWICE today because an
APPEND to an already-checkpointed record post-dates STATE. The lane
carded the question (should an amendment re-trigger the staleness
rule?) and honestly recorded that `35e6504` had already discharged
its instance before the card was written. Queues for the next
sitting.

## Suites (exits unpiped, guarded script, merged main + correction)

parser **315/315** exit 0 (after the census exception) · app
1015/1015 exit 0 · cargo exit 0 · lint:docs exit 0 · health exit 3 BY
DESIGN (7 inside, both docs-headroom bands INSIDE) · e2e **319
passed, 1 failed** — session-economics:73 only, the live-lane class:
`T-163 app/test against T-112 app-board`; greens at T-163's landing,
which is the next act. CI expectation stated before the push: the
no-lane arm now measures GREEN in the CI-shaped clone, so this push
should return main to green.

## Metrics (ADR-020)

Rework: 0. Tokens: executor 192,905 (88 uses, 30 min). Lanes after:
`T-163` only (verifying, held for the flip-set landing: merge + s1
ARCHITECTURE slug block + s2 positive controls + s3 parser fixtures,
one checkpoint). Port 1420 untouched.
