# Checkpoint: T-167 (2026-08-30, integrator nputer-4e, direct pipeline)

Genesis loads the organization's skill packs — the playbook's Stage-2
sentence is now real code: packs discovered at
`.claude/skills/<name>/SKILL.md` in the OPENED project, parsed in the
playbook's format verbatim, carried into the kickoff beside the kit,
and stamped by name and content hash into the session record.
Provenance from birth: "which policy shaped this decision" is
answerable from files alone.

## Merge and evidence

Lane tip `86b6a58`, `--no-ff` -> `77fbf52`; ONE conflict, predicted
by the lane with its resolution PROVED before the merge (main's card
a byte-exact prefix of the lane's — re-verified here with `cmp`
before taking the lane's file whole). 10 paths: skills.rs (new, 808),
kit.rs, mod.rs, sessions.rs, agent_runner.rs, the card, four
suggestion cards. The lane's discipline was the day's best: 19
mutants, 19 one-sided reds, 19 sha256-proved restorations, one
mutant (M18) that CHANGED THE BUILD by surviving until the malformed
body asserted the rejection REASON; the no-packs kickoff proven
byte-identical against text transcribed from the base ref, not
against this build's own sibling function. Two deviations carried on
written reasoning (frontmatter requires name+description with `when:`
optional — compatibility IS the import UX; a hand-rolled SHA-256
pinned by published vectors, blake3 being fenced away).

## Integration corrections, committed-first (this seat's)

Three dogfood pins carry the graph's file census and moved with the
regen the lane rightly left to this checkpoint: file count 189 -> 190,
C-14's per-component row 9 -> 10, the map header hint — one file
joins under C-14, none leave, the unmapped bucket stays closed. All
three re-derived with the movement's reason written beside the
previous movements' reasons, then the full app suite re-run green.

## THE ALARM, routed at priority 1

The graph regen lands at **1,032,605 of 1,040,000 bytes — 99.3%,
7,395 left** (the budget line, this checkpoint's regen). About six
files of headroom at measured density. The ruled fix is T-140-s1's
rollup (blocked by T-135 Half B — now the board's most valuable
unblock); T-167-s2 carries the measurement and three fix shapes.

## Suites (exits unpiped)

Lane: cargo 0 (217 lib · 85 integration · 190 index), boot gate 0,
parser 0, app 0, e2e **320/320**, lint:tokens 0, lint:docs 0.
Integration battery after all three merges: parser 315/315 · e2e
**320 passed** exit 0 · cargo 0 · lint:docs 0 · app 1015/1015 exit 0
re-run after the pin corrections · graph REGENERATED and CURRENT ·
health `--readings` at the final tree: 7 inside, **1 drifting —
triage/live-suggestions at 22 against the 20 drift line** (the day's
lanes filed faster than sittings consumed; standing sitting #2 is
due), 2 unread, 4 UNKEPT, exit 3 by design.

## Metrics (ADR-020)

Rework: 0 in-lane; 3 pin corrections at integration (the regen
class). Tokens: executor 290,852 (138 uses, 40 min). Gate runtime:
the battery's e2e reporter line reads 3.3m (`162-e2e.out`, this
checkpoint's readings input). Cold start: NONE. Drift incidents:
NONE this checkpoint (denominator: 3 merges). Lanes after: NONE.
Port 1420 untouched.
