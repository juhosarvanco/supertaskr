# Checkpoint: T-154 (2026-08-29, integrator nputer-4e, direct pipeline)

The guard merges, and this merge is the arming switch: from this
commit every session in every checkout runs the PreToolUse hook —
non-lane checkouts allow silently (measured at zero bytes on both
streams), lanes are fenced at the moment of the write.

## Merge

Parents `b650f62` (main) and the lane tip only, `--no-ff`. Forecast
exit 0; the merge's diff: **11 paths** — the two hook files and
settings, the manifest writer and its spec, CONVENTIONS' guard
bullet, the card and its three suggestions. `method/` 0 files, as the
card's own criterion routed.

## The verdict's assigned corrections, performed here

- The card's opening claimed three incidents blocked; the verifier
  ruled it a CARD defect ("one of three" — the other two are
  integration-seat writes reaching in, `T-154-s2`'s scope). Corrected
  on the card with the ruling cited.
- CONVENTIONS' bullet declared three limits where the hook declares
  four; the FAIL-OPEN limit is now disclosed in place — proven at
  verification to be the harness's hook contract (a script that
  cannot be located cannot exit 2), reachable only with
  `CLAUDE_PROJECT_DIR` unset AND cwd outside any hook-carrying
  checkout, both at once.

## Gates

- GRAPH REGEN: 0 of 11 paths carries an indexed suffix; ASKED anyway
  at verification and here: CURRENT, 98.1%, 19,977 left.
- BOOT GATE: 0 of 11 — not owed.
- DOCS GATE: FIRES on 5 docs paths, 4 suites named, all green below.
- capabilities: REGENERATED here as `T-154-s3` demanded — Census
  233 → **258 = the runner's own count** (256 extracted + 2 named);
  `capabilities:check` CURRENT after. s3's instance discharged with a
  closed_by line; its CLASS (a lane or CI saying so) stays open for
  triage.
- CONVENTIONS budget: the warn FIRED on this card's own
  documentation, as its criterion predicted; disposition is ADR-019
  Addendum 3 — re-landed at 110,342 bytes, all growth ruled rule
  text, gate values re-derived (warn 137,928 / fail 165,513),
  DOC_BUDGETS updated in the same commit.

GRAPH, asked LAST: after this record's final write — CURRENT,
1,020,023 bytes, exit 0.

## Suites (counts and exits, unpiped)

cargo 518/0/4 exit 0 · parser 314/314 exit 0 · app 1013/1013 exit 0 ·
e2e **258/258** exit 0 (port 16018, lsof zero rows first — the suite
grew by the guard's own 25 bodies) · typecheck 0 · lint:tokens 0 ·
lint:docs 0 after the addendum (warn resolved by re-derivation).

## Environment

Lanes after this checkpoint: `T-153-s2` (executor running) and the
triage branch. T-154's worktree REMOVED, branch kept. Port 1420
untouched; `z` unmoved.

## What the brief got wrong, and what the integrator got wrong

The verifier disclosed that MY dispatch instruction ("read the
executor's notes") partially lifted its informational blindness —
future verifier briefs should demand the attack set be formed from
the card ALONE before the notes are opened. Two-phase verification
enters T-159's review-reconciliation text as a candidate. And this
integrator created a junk duplicate-id file by GUESSING a suggestion
card's filename instead of deriving it (`ls` first — the file was
never staged, deleted in the working tree, the real `T-154-s3`
discharged properly); plus one perl that silently no-opped on a
status the executor had already moved to `verifying`. Both are the
derive-don't-guess class, integrator's hand, stamped here for the
eval corpus.

## Metrics (ADR-020)

Rework cycles: **0**. Tokens, stamped from the meters: executor
350,838 + verifier 222,050 ≈ **573k**. Three clean-vs-rejected
datapoints now exist: 484k (T-153, clean), 573k (T-154, clean,
guard-class), 795k (T-158, one rejection). Gate runtime: not
derivable here (T-157 pending).

## Dispositions

Stamped: T-154 done / same-model, both hands recorded. Arriving:
`T-154-s1` (the .gitignore home), `T-154-s2` (the
fenceless-seat-reaching-in class — needs every-lane manifests and a
ruling), `T-154-s3` (instance discharged here, class open). The
verifier also routed on the card: two undiscriminated producer
branches in the hook, a symlink-aliasing wording gap, and — outside
this card — the drill's own-CARGO_TARGET_DIR advice colliding with
`index --check` in two measured shapes, which CONVENTIONS does not
yet name.
