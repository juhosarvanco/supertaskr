# Checkpoint: T-025-s4 lands — the effective grant is three named tables, one reviewed — and this session's ledger closes

Date: 2026-08-30. Seat: integrator. Scope: T-025-s4 merge and close
with both verdict corrections; the graph at its new floor; the
STATE-staleness catch; the hand-off to the next architect session.

## T-025-s4: APPROVED WITH ASSIGNED CORRECTIONS, landed

The card's central sentence is now mechanical and TRUE: **the
spawned planner's effective grant is the union of three tables —
the adapter's six reviewed patterns, the CLI's own defaults
(measured off the 2026-08-19 capture's init line: the full tool
set; --allowedTools is an AUTO-APPROVAL list, not a restriction),
and whatever the user has configured (read live, never asserted —
it drifted AGAIN under observation, gaining Bash(git merge:*) in
eleven days) — and only the first is reviewed.** Landed as
adapter::EFFECTIVE_GRANT_TABLES with each table naming what the
captures cannot answer. The ruling, dated: the six patterns STAY —
mkdir is not avoidable (stage 0 wants genuinely empty directories),
dropping cp unpaired buys eight in-band denials on a user's first
genesis, and the pin proved a live alarm (deleting Bash(cp:*) reds
four bodies). T-025 criterion 2's containment argument survives
only as a claim about acceptEdits' cwd scoping and is retracted as
a claim about the grant as a whole, in the adapter's own doc
comment. Four questions routed to @human on the card (table-2
narrowing, --setting-sources, cpio boundary, cp-paired-with-kit).

Two corrections performed at merge: (1) ruling reason (c) now
carries its own counterweight IN adapter.rs — smallest by count is
largest by write reach; (a) and (b) carry the ruling; (2) routed
item 1's obstacle restated — the lane's own table 2 refuted the
original wording; the routing stands on the real risk (a different
installed CLI build's argument validation), which it does.

**The verifier disclosed its own blindness violation** — the card
came back in full before its attack set was written — recorded it
in its attack file, and compensated by re-deriving every figure and
aiming mutants at the capture side the executor never touched (M1
mutated the capture FILE and the fidelity body redded printing the
file's mutated bytes: the second-implementation question answered
by construction). Five mutants, five killed. Fence clean. And it
watched the live-lane class CLOSE in real time: the e2e refusal it
reproduced at base exits 0 at the same tree now, because T-163-s4
merged mid-verification.

## The graph's new floor

Regenerated at the merge: **1,039,590 of 1,040,000 — 410 bytes
left**, 199 files, 2067 symbols. The alarm prints; the check exits
0; nothing degrades further today. **T-140-s4 is no longer
deferrable: any code merge that grows the graph by a few hundred
bytes crosses.** The ruling is @human's; the next architect session
inherits this as its first constraint.

## The gate caught this seat, and that is the system working

The close battery redded three docs-input-gate bodies and lint:docs
— cause: docs/STATE.md was STALE against the T-163-s4 record (this
seat committed that record without regenerating STATE;
docs-protocol rule 4). Third sighting of the record-append class
(T-143-s5's), FIRST caught mechanically before a push. Repaired in
this same commit: one STATE rewrite closes both records.

## The session's ledger, closed

Landed today by this session: the seven morning rulings; T-167-s1;
T-154-s2 (the guard's second arm — the first integration shaped by
the guard it landed); T-112 (F-04 whole); T-163-s4 (the live-lane
red class ENDED); T-025-s4 (this record); the genesis walk prepped,
briefed, debriefed — T-171/T-172/T-173/T-174 filed with the
cold-start capture; the marketing session's 13 rulings folded; the
charter, business layer, version-planning room. Incidents against
this seat, all recorded at their moment: the 18d8166 false commit
message; the STATE staleness above. ZERO LANES live. **The takeover
signal for the new architect session (per @human's instruction and
the standing chip) is FIRED: no task/ worktrees, all cards done on
main.** Its first items: the triage sitting for the walk's cards +
T-112-s1..s4 + T-154-s3/s4 + T-163-s5, and T-140-s4's ruling from
@human before the next code merge.
