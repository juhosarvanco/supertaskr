# Checkpoint: T-153-s9 (2026-08-30, integrator nputer-4e, direct pipeline)

The lane instrument stops manufacturing its own reds: the integration
branch is RESOLVED (bare name first as the safety property, then
origin/main, then the full remote ref; a named refusal when nothing
resolves — never a silent HEAD), and the 29 PR-checkout failures are
gone, 28 proven green on a real pull_request run and the 29th
reclassified and routed (T-153-s14).

## Merge

Parents `0d63006` (main) and lane tip `3d94c1c`, `--no-ff` ->
`a5f3abe`. 9 paths: dispatch-brief.mjs (the resolver; row 4 now
emits the resolved ref as a LIVE fact), card-figures.mjs (provenance
names the resolved ref, the figure TEXT keeps the branch name),
brief.spec.ts (three built checkout shapes + the repaired provenance
regex), the card, five suggestion cards (s13..s17). Verdict:
**APPROVED WITH ONE ASSIGNED CORRECTION**, performed here: the
`spendsBranch` lookahead widened to admit revision-operator suffixes
(`^~:` and `@{`) — the verifier planted a `main^{commit}` provenance
stamped TREE that the narrow class dropped and the suite passed;
validated red-with-plant / green-without before assignment, re-run
green here (42/42 across both corrected specs).

## The three-copy alignment (T-153-s15, discharged here)

The s13 cargo-audit guard had left ONE command spelled two ways
across four copies. Aligned in this checkpoint the moment the merge
freed the fence: ci.yml (already guarded, on main), CONVENTIONS'
two mentions, the parity spec's CI_SEQUENCE declaration, and the
fixture expectation — workflow-parity green end to end after. s13
itself was discharged by `129e3c9` (landed direct under the CI-green
authorization); both cards carry dated closed_by lines, arriving and
closing in the same merge.

## Gates and suites (exits unpiped, combined tip)

e2e **283/283** exit 0 · parser 314/314 · app 1015/1015 · cargo all
ok · capabilities REGENERATED (22,190, CURRENT — the two new spec
bodies) · lint:docs 0 · lint:tokens 0 · GRAPH REGEN: performed at
this sitting (T-140's record carries it), asked LAST after both
records' final writes — CURRENT, 1,022,964 bytes, exit 0.

## Seat notes for the eval corpus

- The verifier's fourth checkout shape (local diverged from remote)
  confirmed the resolver's doc-comment claim the suite never drove;
  its own first docs-gate run refused one of its own cards (a
  colon-space plain-scalar title — 9c64cd8's shape), fixed and
  recorded rather than quietly repaired.
- The parity chain caught the INTEGRATOR twice in one night: the
  ci.yml guard broke step-verbatim parity (three named copies + a
  fixture's fourth), and the whack-a-mole stopped only when the
  spec's own derivation was READ instead of probed. Two-copies
  discipline is not advisory.
- Routed and standing: T-153-s14 (the one-Checkpoint read that does
  not reproduce — its precondition now discloses everything, one PR
  run answers it), s16 (candidate-list edges), s17 (the missing
  `--` separator), T-161 (the stderr-drain intermittent, 1-in-2 on
  the green-run day), T-140-s2 mooted in practice by the narrowed
  regex but kept for its local-vs-CI divergence note.

## Metrics (ADR-020)

Rework cycles: 0. Tokens: executor 337,850 + verifier 291,973 ≈
**630k**. Lanes after this checkpoint: NONE — next per @human's
ruled night order: T-160 (the dispatch preflight; its tools/e2e
fence frees here), then T-157, then T-159. Worktree REMOVED, branch
kept. Port 1420 untouched; `z` unmoved. Main pushes from this
checkpoint; the run it triggers is watched with T-161's intermittent
on the books.
