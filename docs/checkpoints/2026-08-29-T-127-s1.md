# Checkpoint: T-127-s1 (2026-08-29, integrator nputer-4e, direct pipeline)

The cycle is NOT broken, and that is the deliverable: the lane proved
its own title one generation later. T-149 moved the two dogfood
fixtures out from under the fence (the `app/test/**` glob became
sixteen named files routed to C-12/app-map), and a THIRD enforcing
copy no criterion ever named — `KNOWN_DECLARED_CYCLES` in
crate-index's arch.rs — was outside every fence this family has
carried. The lane refused to widen, measured all three candidate
partitions instead, and routed the work to a fence that can reach it.

## Merge

Parents `95cf2d0` (main) and lane tip `3e41e87`, `--no-ff` ->
`ecb852b`. The merge's diff: **2 paths, docs/tasks/ only** — the card
(+measurements) and the successor `T-127-s6` (fence:
[docs/architecture/components/, app-map, crate-index], carrying the
VERIFIED four-node partition and its exact red set: 4 reds in
architecture-dogfood, 2 in map-dogfood-render, plus `cargo test
--test arch` 101 on the stale allowlist).

## Provenance — why no verifier seat sat

The diff is knowledge-only (two cards). Every measurement it records
is re-derived BY CONSTRUCTION in T-127-s6's own lane, whose criteria
carry the same partitions — a verifier here would verify claims the
successor must re-prove in-fence anyway. Stamped same-model,
self-reported, ceremony scaled to blast radius (T-135's rule); the
successor's lane gets the full verifier seat.

## Gates and suites (exits unpiped)

DOCS GATE fires on 2 docs/tasks paths — e2e **281/281** exit 0
(`NPUTER_E2E_PORT=14670`), parser 314/314 exit 0, app 1013/1013 exit
0, lint:docs exit 0. GRAPH REGEN not owed (asked LAST after this
record's final write — CURRENT, 1,021,562 bytes, exit 0). BOOT GATE
not owed. `arch cycles` remains exit 1 BY DESIGN — the headline
retires when T-127-s6 lands, not before.

## What the dispatcher got wrong, stamped

- The brief's ROW 4 printed the worktree as `nputer-T-127-s1`; the
  real one was `nputer-T-127s1` — MY naming deviated from the
  published spelling. Future lanes use the published form.
- The card's fence was stale at dispatch (app-shell for fixtures that
  T-149 had rerouted to app-map) — the dispatch derivation read the
  card's `touches:` faithfully and the CARD was behind the tree. The
  class (a fence line that does not move when files move out from
  under it) is now written on both cards.

## Metrics (ADR-020)

Rework cycles: 0 — the honest stop is the deliverable, not rework.
Tokens: executor **163,599** (70 tool uses, 14 min) — the cheapest
lane arc yet, and it bought the measurement that saves the successor
from repeating three drills. Lanes after: T-153-s5 (executor mid-CI),
T-127-s6 dispatching now. T-127-s1's worktree REMOVED, branch kept.
