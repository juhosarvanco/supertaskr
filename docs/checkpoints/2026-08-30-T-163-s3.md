# Checkpoint: T-163-s3 (2026-08-30, integrator nputer-4e, direct pipeline)

The C-11 ruling's LAST live false copy is corrected — CONVENTIONS'
SHIPPED PARTITION bullet now states the ruled truth ("claims NO slug
since T-163, and ships anyway"), and the lane's sweep earned its keep
twice over: a SECOND false clause the card never named (the
"one EMPTY line (C-01)" claim — two components carry the empty list,
now derived), and the DERIVED answer to what carries shipped-ness once
a slug is gone (a bare path into a slugless component whose bytes
REACH the built app — confirmed in the emitted bundle, stated
explicitly as non-generalising). All CONVENTIONS readers green; the
lane's one e2e red proven the base's (the T-169 lane's existence),
both directions. Negative sweep recorded: no other clause assumes a
doubly-claimed component. T-163-s4 filed (the session-economics
positive control graded on a collidable live card id). Merged
--no-ff, stamped done, verified by the integration seat (docs-only).
Suites: the shared battery at this checkpoint (T-135's record carries
the line). Tokens: executor 147,501.
