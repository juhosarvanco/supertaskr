# Checkpoint: T-153-s6 (2026-08-29, integrator nputer-4e, direct pipeline)

The last carded Linux red dies: the xargs dialect is PROBED at run
time — two observables through the recipes' own shell, expectations
pinned to DISAGREE per dialect, a third dialect refused by both
consumers — and the shipped mistake now reds on the platform that
could not observe it. THE PUSH FOLLOWS THIS CHECKPOINT: every red
main's Linux runs have ever shown is fixed and pinned.

## Merge

Parents `46dd8f8` (main) and lane tip `164d311`, `--no-ff` ->
`612763a`. 8 paths: xargs-dialect.mjs (new prober — no
process.platform anywhere), range-rule.mjs (columns read from the
HEADER, the doc's dialect set compared to the prober's, empty-list
cells required to diverge and $() columns to agree),
docs-input-gate.spec.ts (both dialects side by side, red on unknown),
CONVENTIONS (two false statements corrected; the hazard now
platform-scoped and DIFFERENT per dialect — BSD hides a failed range,
GNU destroys the codes' identity), docs-gate.mjs's header (the second
copy, same correction), the card, and two suggestion cards (s11 the
twice-told-story drift class, s12 the prober's second-copy number —
filed by the verifier). Verdict: **APPROVED WITH TWO ASSIGNED
CORRECTIONS**, both card-notes accuracy, performed here: the drill
table's mutant now pairs with the message it actually produces, and
the owed-suite record names all FOUR suites the gate names (the task
cards are docs inputs too), with the verifier's own figures.

## CI evidence (three runs, per-body, verifier-confirmed)

Both target bodies GREEN on ubuntu in all three lane runs —
33271000696, 33272004368, and 33272976860 (the tip's own run, which
completed after the executor wrote; the verifier read it as free
evidence). Every remaining PR-run red matches T-153-s9's enumeration
line for line. The runner stamped its own dialect measurement into
each run. Draft PR #4 closes when main pushes.

## Gates and suites (exits unpiped, merged main)

cargo 522/0/4 exit 0 · parser 314/314 exit 0 · app 1013/1013 exit 0 ·
e2e **281/281** exit 0 (`NPUTER_E2E_PORT=14700`) · lint:docs exit 0
(CONVENTIONS 117,399 of warn 137,928 — 20,529 headroom) ·
capabilities CURRENT (21,992) · GRAPH REGEN: fired by the literal
trigger, provable no-op (tools/ is ignored; bytes identical), asked
LAST after this record's final write — CURRENT, 1,021,562 bytes,
exit 0 · BOOT/METHOD-EVAL not owed.

## Seat notes for the eval corpus

- The verifier's own extra drill — a shim GNU to the prober and BSD
  to the real gate — closed the could-they-disagree question by
  measurement: both consumers catch the split ("one of the two
  measurements is wrong and neither may be trusted").
- Eight verifier mutants: seven red, one green (the green is
  T-153-s12's finding — a second-copy number that fails loud on the
  platform it is wrong about, so routed rather than assigned).
- The executor corrected two of its own readings in flight (the
  timeout-vs-docker misread; the base-hash coordinates) — the
  derive-don't-guess discipline running inside one seat.

## Metrics (ADR-020)

Rework cycles: **0**. Tokens: executor 249,496 + verifier 173,885 ≈
**423k** — the cheapest verified guard arc yet. The T-153 family is
now CLOSED on the board's critical path: parent + s2 + s5 + s6
landed, s9 next (PR instrumentation), the rest routed or riding.
Lanes after this checkpoint: NONE. Worktree REMOVED, branch kept.
Port 1420 untouched; `z` unmoved. MAIN PUSHES NOW — watched as a
first run.
