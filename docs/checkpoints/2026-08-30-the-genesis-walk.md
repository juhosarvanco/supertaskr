# Checkpoint: THE GENESIS WALK — @human walked a real project into existence, and the flow's missing ending is the finding

Date: 2026-08-30. Seat: integrator, with @human at the app. Scope:
milestone 3's held item performed; three cards filed from the
debrief; the cold-start test run by hand; T-025-s4 unheld.

## The walk

@human ran the launcher, restarted the app on main `d5c4b65`, and
walked a genesis end to end at `/Users/ujju/Projects/first-walk`:
seven questions (answers drafted light by design — a zero-dependency
daily-note opener), every bank landed, ~14 minutes elapsed, and the
board pane finished holding three cards — T-001 S (executable
skeleton + today's-path resolution), T-002 M (create without ever
destroying), T-003 M (launch $EDITOR) — under "milestone 1 ships
above". On disk, verified by this seat: the full docs/ set
(NORTH_STAR, ROADMAP, ARCHITECTURE, CONVENTIONS, STATE, decisions/,
tasks/ with TASK-FORMAT and the T-000 template, rooms/,
checkpoints/), CLAUDE.md + AGENTS.md at the root. The planner ran on
`claude-opus-5[1m]` (ADR-003: the CLI's default IS the model; the
init line reported it). The planner's challenge treatment worked as
designed — it pushed back on the first-slice scope, folded two
questions into one ask, and *overturned one of its own `[?]`
assumptions when @human's answer contradicted it, saying that is what
the marks are for*.

## The findings, each now a card

1. **T-171 — genesis has no ending.** After the last bank the
   planner named the method's own cold-start test, honestly said it
   cannot be the fresh session the test requires, and the footer
   held "planner is thinking…" forever. @human: "I don't know how to
   move forward from it." The method's last stage has no operational
   owner in the app. This seat ran the test by hand (fresh Opus
   session restricted to first-walk/docs/, explain-back demanded);
   result lands on T-171.
2. **T-172 — two chrome rulings, @human verbatim**: the
   "one question at a time · N of 7" line under planner messages is
   unnecessary; "Bank answer" should be just "Answer".
3. **T-173 — the ruled UX direction**: free write-up first, planner
   drafts every stage's answer from it (accept/edit/own-words, skip
   still banks a `[?]` draft). At this walk the integration seat WAS
   that feature, by hand, drafting all seven answers in chat.

## Consequences

- **The walk is DONE.** Milestone 3's closing word is @human's; this
  seat's read is that genesis produced a correct, whole project and
  the ending gap is filed rather than fatal.
- **T-025-s4 is UNHELD** — the one walk-held card (Bash allowlist
  narrowing) is dispatchable from this record on.
- T-112's blind verifier, held quiet during the walk, is dispatched
  (with the T-154-s2-pattern two-phase protocol).
