# Checkpoint: T-112 lands — a card hands you its brief, F-04's last act, and the graph klaxon is now a fact on disk

Date: 2026-08-30. Seat: integrator. Scope: T-112 merge and close with
its three verdict corrections; the dogfood re-derivations at 199
files; the graph budget's new resting place; what is next.

## T-112: APPROVED WITH ASSIGNED CORRECTIONS, landed

The dispatch-brief assembler is in: thirteen rows parsed out of
`method/roles/executor.md` AT RUN TIME (nothing anywhere transcribes
a count — a fourteenth row returns Unassemblable naming number and
source, an empty table is a typed ContractDefect), withholding by
construction (ENTITLED_FIELDS excludes the executor's reasoning for
every role — there is no flag to set wrong), refusals a serde-tagged
sum type that cannot represent a partial brief, and
selectDispositions' first consumer wired (selectBriefPanel). The
blind verifier — 45 attacks written before facts, 12 own mutants, 9
killed — called it "the strongest piece of work I have verified on
this repository". Its two material survivors became the corrections:

1. **The wrong holder, shipped into every brief.** row_deliverable's
   residual said "Filed as T-112-s3" where the holder is T-112-s2 —
   the executor's own notes had it right and the string did not, and
   V-M7 proved no body pinned the citation. Fixed + two holder pins.
2. **FIRST unguarded.** notes_boundary correctly takes the first
   heading, but every fixture carried one heading, so .find →
   .last() survived 28 bodies. On any card carrying both headings —
   every card that has been through a verdict — the last-heading
   variant reads the verifier straight through the reasoning
   criterion 5 exists to stop. Two both-heading fixtures added.
3. Two tallies restated at their ref on the card (cargo 588/0/4 at
   96e3886; CONTROL 981 at 96e3886).

Fence 13/13 clean against the registry's own touch_slugs fields. The
T-112-s1 routing (registration + census pins are app-shell, outside
the fence) judged CORRECT by the verifier against TASK-FORMAT's own
paragraph — the card was the defective party, the burden triage's,
and the lane took the one legal move. brief_for_card is in the
BINARY crate (T-126's hazard closed).

## The graph klaxon, realized

Regenerated at the merge (three times — after the merge, after the
pin edits, after the tally edit; asked after every write):
**1,039,074 of 1,040,000 bytes — 926 left**, 199 files, 2065 symbols
(-30: truncation deepened), 2334 edges, truncated_files 2→4,
docs_watch.rs 55→0 symbols. The alarm prints, the check exits 0,
degradation lands on drill-not-map by T-140-s1's design — and the
room is now under one kilobyte. **T-140-s4's ruling (@human: raise
the limit, or bless deeper truncation, or something else) is the
next merge's problem if it moves the graph at all.** Dogfood pins
re-derived with the story: 198→199, C-15 five→six files (brief.rs),
C-09→C-17 4→7 (selectBriefPanel's seams), header hint, membership
array, per-component tally.

## Gates at the close

cargo 588/0/4 · app 1060/1060 (49 files) · e2e 332 passed exit 0 ·
parser 336 · typecheck/lint:tokens/lint:docs 0 · graph CURRENT.
capabilities:check remains red on main and remains T-153-s8's by
name (no tools/e2e path in this merge).

## Board

T-112 done (verifier fields stamped, review empty — M ceremony
honoured: executor, verifier, integrator all distinct seat
instances). Its four suggestions are on the board: **T-112-s1 is the
natural next dispatch** — until it lands the assembler is reachable
only from cargo test and the drawer's block never renders (honest
absence, by design). Also queued: e2e train (T-163-s4 p1), T-025-s4
(unheld). @human's open items gain nothing new — but note the card's
own closing evidence line is @human's: **one hand-driven dispatch of
a real card using only this brief**, which pairs naturally with the
next real card @human wants built.

F-04 is whole: disposition, ledger, guard, brief — the follower-first
slice's last act.
