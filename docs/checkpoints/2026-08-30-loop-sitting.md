# Checkpoint: the loop-customization sitting (2026-08-30, @human with integrator nputer-4e)

Three more @human rulings landed the same day they were asked — the
afternoon sitting, following the morning's seven.

## The rulings

1. **Milestone 3 is HELD FOR @HUMAN'S OWN WALK** — @human will drive
   one full genesis (Q1–Q7 to a materialized board) in the real app
   and verify it with their own eyes; the automated turn's evidence
   stands, and the milestone closes on the walk. No hurry; the
   machinery is ready (auth live, the deadline fixed). ROADMAP's gate
   paragraph already routes this correctly.
2. **The loop-customization sitting: APPROVED, two tracks, FORM
   FIRST** (binding, verbatim in rooms/loop-customization.md): no UI
   visual design work before the customization FORM is ruled —
   config system, in-app setup area, or designed hybrid, thought
   from the ground up.
3. **D5 RULED in @human's own words**: "of course the models the
   human assigns to different tasks do those tasks as assigned" —
   assignment is BINDING; adapters enforce where they can, nputer
   verifies everywhere, and a mismatch is a violation flagged on the
   board, never a silent substitution
   (rooms/cockpit-or-mirror.md).

## The sitting's cuts, every one preflighted at dispatch

- `T-167` (planned, priority 2, app-agent) — genesis discovers
  `.claude/skills/<name>/SKILL.md` packs in the opened project,
  carries them into the kickoff, stamps names and hashes into the
  session record. Form-independent plumbing; the playbook's Stage-2
  sentence made real.
- `T-168` (planned, priority 1, docs only) — the industry benchmark
  sweep plus THE FORM DECISION BRIEF, routed to @human; GATES every
  UI card in the feature.
- `T-169` (planned, priority 4, lib-parser + app-board) — D5's
  teeth: builder/built_by and verifier/verified_by compared on the
  model half; a disagreement is flagged like a parse error.
- `T-166` DONE — the hook closed at the sitting it existed to hold,
  its resurfacing condition fired within hours of filing.

## Dispatches leaving this checkpoint

`T-167` ∥ `T-168` ∥ `T-156-s1` (the health-bands checkpoint carry,
queued since dawn on the CONVENTIONS runway T-162 restored) — fences
pairwise disjoint, derived at dispatch, all Opus per standing rule.
`T-169` queues behind the board seat; `T-112` remains dispatchable at
ONE collider; `T-164` collides with nothing.

## Context records of the same hour

The Ring 4 addition to the Beyond-the-Playbook charter (@human's
gift-registry directive) lives in the artifact; the in-repo
authorities remain docs/research/ai-native-sdlc-playbook-review.md
and the two rooms. CI at this sitting's open: GREEN through the
T-163 checkpoint (59bdaed), zero lanes, 320/320.
