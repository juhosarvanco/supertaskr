# Checkpoint: T-160 (2026-08-30, integrator nputer-4e, direct pipeline)

@human's card lands: dispatch now PREFLIGHTS the card itself —
`brief.mjs --task T-NNN --preflight` re-derives five claim classes at
HEAD (paths, fence, figures, blockers, refs), refuses in the fence
writer's shape, prints on every run what it checked and what it
cannot, and — since the verdict's corrections — GATES the manifest
write in the same invocation. The tool found a true board defect on
its first live pass (T-059 orders a write its fence cannot reach —
T-160-s4), which is the strongest acceptance a gate can offer.

## Merge

Parents `1c885e2` (main) and lane tip `f3074c9`, `--no-ff` ->
`8f8552d`. 10 paths: card-preflight.mjs (the derivation, zero deps,
consuming context()/readDispatchOrder/auditCard/within — nothing
re-spelled, T-057), card-preflight.spec.ts (18 lane bodies + 2
correction pins = 20), brief.mjs (arm six), CONVENTIONS (the ritual
sentence, deliberately NOT in Build & test, limitation stated in
place), the card, five suggestion cards (s1 no-tripwire, s2
indented-block blindness, s3 whole-brief emission, s4 the live
defect, s5 the zero-byte-diff reading). Verdict: **APPROVED WITH
FIVE ASSIGNED CORRECTIONS**, all performed here:

1. The `cannot:` rows now name their four measured blind spots
   (dead top-level segments, leading-`./`, root files without a
   slash, prose-spelled refs), and `paths.checks` no longer claims
   "every path".
2. **The false claim became the feature**: a failed preflight GATES
   `--write-fence` — no manifest for a card whose claims fell, with
   the clean twin proven in the same invocation shape. Pinned;
   mutant drilled (coarse kill — the mutation pattern also hit the
   exit path; stamped as such), restoration sha256-proved.
3. Rulings discharge at a TOKEN BOUNDARY (`.map` twins and suffixed
   ids no longer over-discharge; a sentence-ending period still
   closes). Pinned; bare-substring mutant red; restoration proved.
4. The ruling's load-bearing FORM (plain unindented body line) is
   stated in CONVENTIONS where authors will read it.
5. Two stale figures on the card re-derived (300, 23,599).

The verdict also RULED the contested ownership-inference arm STANDS
AS BUILT: on the dispatchable board it fired exactly once, a true
positive; done-card noise is operationally irrelevant and the escape
is one dated line.

## Seat notes for the eval corpus

- The tool's own laws caught its integrator twice while correcting
  it: `note()` refused a digit in my gate message, then refused
  "T-160" itself inside the cannot-rows — the no-unstamped-figures
  rule enforcing itself against the hand that built it.
- The founding irony held end to end: T-160's own dispatch caught a
  fence its criteria outran (by hand), and its verifier found the
  write-fence gate missing — the card about preflight discipline
  needed every layer of it.

## Gates and suites (exits unpiped, merged main + corrections)

e2e **303/303** exit 0 · app 1015/1015 · parser 314/314 · cargo 18
result-lines ok · typecheck/lint:tokens/lint:docs 0 · capabilities
REGENERATED (23,770, CURRENT — the 20 preflight bodies) · GRAPH
REGEN: not owed (tools-only code diff; asked LAST after this
record's final write — CURRENT, 1,022,964 bytes, exit 0) ·
BOOT/METHOD-EVAL not owed.

## Metrics (ADR-020)

Rework cycles: 0. Tokens: executor 408,741 + verifier 269,100 ≈
**678k** — the priciest arc of the night and the one that changes
every future dispatch. Lanes after: NONE. Worktree REMOVED, branch
kept. Next per the ruled order: T-157, then T-159, then the first
standing triage. Port 1420 untouched; `z` unmoved. Main pushes from
this checkpoint.
