# Checkpoint: T-159 (2026-08-30, integrator nputer-4e, direct pipeline)

METHOD v0.1.8 — the metabolism release. Ten riders landed (T-152
first, per its own TAKE-FIRST cost), four reconciled back with dated
reasons, three version stamps moved as one commit carrying the eval
block, and the release's own guards caught its own lane twice on the
way in (a reader-red withdrawal; the parser refusing the lane's
illegal card about the very rule it was rewriting).

## Merge

Parents `2a5562c` (main) and lane tip `fae95ce`, `--no-ff` ->
`3def72c`. 19 paths: seven method/ files (lane-protocol, all five
roles, TASK-FORMAT, docs-protocol law 8, plan-interview), both
adapters (cmp-identical twins), CONVENTIONS (+6,688 — 91.6% of warn,
stamped because the CONVENTIONS train is queued), kit.rs's version
pin, the card, five suggestion cards (s1 refused-then-refiled about
rider stamping, s2 outside-fence version claims, s3 adapter lag, s4
the banking-table fence gap, s5 the hygiene-skeleton checker).
Verdict: **APPROVED, zero corrections** — the verifier proved the
method evals fire on the release's own text four independent ways,
re-derived the version census tree-wide, spot-verified five rider
dispositions from their own cards, and settled the hygiene-vs-law-5
question on three grounds stronger than the executor's own (role
files are not governing documents; the five sections carry OPPOSITE
per-seat values; the tooling-shaped worry is refuted by measurement —
`--role` exits 3 for four of five seats, corroborating T-133).

## Integration performed here

- GRAPH REGEN (kit.rs staled it): regenerated, CURRENT.
- **T-159-s2 discharged at the seat**: all three outside-fence
  version claims moved with the bump (ARCHITECTURE's C-01 row, the
  C-01 file's status pin — which was TWO versions stale — and
  ROADMAP's F-01 clause), plus a fourth of the same family found in
  the sweep: ARCHITECTURE's "CI dormant until first push" retired
  with the green-run citation. The class stays s2's if it recurs.
- The verifier's disclosure stands stamped: MY dispatch brief again
  carried two executor-derived specifics above the marker — the
  THIRD sighting, and the release itself now legislates the fix
  (verifier step 0). The next dispatch complies or the eval corpus
  gets a fourth instance.
- The mid-lane red commit recorded: `9c0da79` in isolation reds
  genesis-derive (1/1014) — the tip is green because `6b37f57`
  withdrew the clause. Contained by the base rule (lanes cut from
  checkpoints, never mid-lane commits); noted for T-159-s4's fence
  argument.
- `z`, the untracked root file, finally gets its record line: it
  predates the compaction, no session this record's author has run
  created it, and it stays untouched per the environment rule. A
  morning human with context may simply delete it.

## Suites (exits unpiped, merged main + seat fixes)

cargo 18 result-lines ok (kit.rs pin at 0.1.8) · e2e **313/313**
exit 0 (`NPUTER_E2E_PORT=14750`) · app 1015/1015 · parser 314/314 ·
lint:docs 0 · capabilities CURRENT (24,849 — no spec names moved) ·
method evals: model-free 0, --bump 0 with the runner named, negative
control exit 3 (verifier's own run) · GRAPH asked LAST after this
record's final write — CURRENT, exit 0.

## Metrics (ADR-020, per the T-157 slot)

Rework cycles: 0. Tokens, read 2026-08-30 on Mac.lan from the
meters: executor 333,423 (213 tool uses) + verifier 303,821 (118) ≈
**637k**. Gate runtime: e2e 3.0m from the runner's own summary;
others not separately derivable here (reason: run interleaved at the
integration seat).

Lanes after: NONE. Worktree REMOVED, branch kept. NEXT: the FIRST
STANDING TRIAGE under the rules this checkpoint just landed — the
`triage/live-suggestions` band has been BREACHED since the T-157
checkpoint, which is the system itself demanding the sitting. Port
1420 untouched. Main pushes from this checkpoint.
