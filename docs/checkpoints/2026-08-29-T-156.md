# Checkpoint: T-156 (2026-08-29, integrator nputer-4e, direct pipeline)

The method watches its own health from this merge: 14 bands as data,
each carrying its measured reason, the four doc-headroom bands DERIVED
from DOC_BUDGETS so a document that gains a budget gains a band. The
first official reading did its job before the ink dried — see Metrics.

## Merge

Parents `40d03b7` (main) and lane tip `233a827`, `--no-ff` ->
`a21dc5c`. 12 paths: the three health-bands files, the spec (21
bodies), package.json's `health` script, DOC_BUDGETS' move
docs-gate.mjs -> docs-scan.mjs (one table; the gate imports it — it
executes at import, so a second reader could not live there), the
card, four suggestion cards (s1 checkpoint-carry, s2 NORTH_STAR
recording mechanisms, s3 token-scan dirty-tree hazard, s4 the
exit-code design routing). Verdict: **APPROVED WITH FOUR ASSIGNED
CORRECTIONS**, performed at `1001791`:

1. `findingCard` now emits a QUOTED YAML title — measured both ways
   at verification (a ": " made the card unparseable, a " #"
   truncated it silently); hostile-title fixture added to the LEGAL
   suggestion-card body.
2. The null-budget guard got the table its keeper needed (the drill's
   one survivor): `readingsFromTree` gained a `budgets` seam
   defaulting to DOC_BUDGETS, and a fixture drives a null-bearing
   table through both consumers.
3. The moved table's three LIVE pointers now point home (ADR-019
   addendum, STATE-template, T-111-s10) — records deliberately kept
   their old spelling.
4. Handoff-figure hygiene: the base ran 258 specs, not the executor's
   259 (the verifier proved 259 unproducible at that base under any
   env spelling); capabilities fresh was 21,795 pre-correction.

Correction drills, committed-first in this checkout, producer mutated
never the assertion: null-guard deleted -> the new fixture reds
(1f/21p); quoting reverted -> the hostile fixture reds (1f/21p);
restorations sha256-MATCH HEAD (`da2f23df200d`), re-run 22/22.

## Gates

- DOCS GATE: exit 0. lint:tokens exit 0. typecheck exit 0.
- capabilities: REGENERATED here (21,886 bytes, CURRENT) — the
  T-154-s3 class instance the executor correctly left to this seat.
- GRAPH REGEN: 0 indexed suffixes; asked LAST after this record's
  final write — CURRENT, 1,021,562 bytes, exit 0.
- BOOT GATE: not owed.

## Suites (counts and exits, unpiped)

e2e **280/280** exit 0 (`NPUTER_E2E_PORT=14630` — the variable's real
name, derived by the verifier from preflight.ts; the E2E_PORT spelling
two earlier batteries exported binds nothing) · parser 314/314 exit 0
· app 1013/1013 exit 0.

## Metrics (ADR-020) — and the FIRST checkpoint health reading

`npm run health` exit 3: **5 inside, 1 drifting, 1 BREACHED, 3
unread, 4 unkept.** The breach is `docs-headroom/docs/STATE.md` at
1.10% of the warn line — the reporter's first official reading caught
the very document this checkpoint regenerates, and the regen below is
the remedy; ROADMAP drifts at 4.12%. The 3 unread need a captured-
output run (`--readings`); the 4 unkept await T-156-s1/s2. Exit-code
design routed on T-156-s4.

Rework cycles: **0**. Tokens, stamped from the meters: executor
261,709 + verifier 251,019 ≈ **513k**. Arc series: 484 / 573 / 795 /
477(140 cards) / 530 / 513k.

## The integrator's own slips, stamped for the eval corpus

- The verifier disclosed my dispatch-brief FORMAT lifted its
  blindness: the duties section named executor-derived specifics
  (mutant numbers, counts) above the phase-2 marker. Both verifier
  briefs this sitting carried the flaw; both verifiers disclosed and
  contained it. Fix for the next dispatch: duties stay generic, every
  executor-derived fact goes below the marker. T-159 material.
- Four cwd slips in one sitting (git pathspecs from tools/e2e twice,
  npm scripts from app/ once — the Missing-script exit 1 that STATE
  warns reads like a failure). The class is documented; the hand
  still pays it. Also eval-corpus material.

## Environment

Lanes after this checkpoint: T-155 only (verdict in — APPROVED WITH
ASSIGNED CORRECTIONS, tip 2b0ee6b; its merge is next). T-156's
worktree REMOVED, branch kept. Port 1420 untouched; `z` unmoved.
Main NOT pushed — the push waits for T-153-s5/s6 so the next CI run
can be the first green one.
