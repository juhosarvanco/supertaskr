# Checkpoint: T-158 (2026-08-29, integrator nputer-4e, direct pipeline)

The first merge through the resumed pipeline, and the first through
the ADR-020 merge gate exercised as designed: @human pre-authorized
the merges in chat ("merge them when they are ready"), the permission
layer allowed this one, and every claim below was derived at this
integration.

## Merge

Parents `3607a94` (main) and `23ba609` (lane tip) only, `--no-ff`,
merge commit is main's tip at this record. Forecast
`git merge-tree --write-tree main task/T-158-human-front-door` exit 0,
tree `c46e6e5` — the merge's tree, byte for byte. The merge's diff
(RANGE RULE at-merge pair): **2 paths** — `A README.md`,
`M docs/tasks/T-158-the-human-front-door.md`. Disjoint from both live
lanes (T-153 `app-shell`, T-154 `.claude//tools/e2e/CONVENTIONS`) by
construction and by the dispatch-time check.

## Gates

- GRAPH REGEN: trigger matches 0 of 2 paths (no indexed suffix);
  ASKED ANYWAY: CURRENT, 1,020,023 of 1,040,000 bytes (98.1%),
  19,977 left.
- BOOT GATE: 0 of 2 paths — not owed, not run.
- DOCS GATE: FIRES on the card (9 readers, 3 suites named); README
  itself is read only by the two walk-everything specs. All owed
  suites run, green.
- capabilities:check ASKED: CURRENT (18,133 bytes) — T-154's suite
  growth is lane-local until that merge; the regen debt transfers to
  T-154's checkpoint.

GRAPH, asked LAST: after this record's final write —
`graph.json is CURRENT ... (1020023 bytes, 189 files, 2152 symbols,
2111 edges)`, exit 0.

## Suites (counts and exits, unpiped)

parser 314/314 exit 0 · app 1013/1013 exit 0 · e2e 233/233 exit 0
(port 16016, lsof zero rows before the bind) · lint:docs exit 0
(4 budgets hold) · lint:tokens exit 0 (TOKEN 144 / CONTROL 846) ·
cargo not owed (no `.rs`, no CONVENTIONS change) and not run.

## Board

`T-158` is the only card this merge stamps: `building` → `done`,
`built_by`/`verified_by` claude-opus-5@subagent, `review: same-model`
(separate sessions, same weights — @human chose uniform Opus with the
provenance consequence stated at dispatch). The card carries the full
arc: REJECTED (a false sentence about verification itself, on the
front door) → fix pass deriving the truth phrase-by-phrase from
verifier.md and TASK-FORMAT.md, catching a THIRD false row via the
verdict's per-row-citation recommendation → APPROVED with every
citation re-opened. Board censuses move under live lanes; derive at
your own ref.

## Environment

Two lanes live at this record (T-153 at `fada695` verdict pending,
T-154 at `8ccdfc1` verdict pending), both read from
`git worktree list --porcelain`. T-158's worktree REMOVED after this
checkpoint (rule 6), branch kept. Port 1420 untouched. The untracked
`z` remains in the main checkout — not this integration's, left
alone.

## What the brief got wrong

The dispatch brief said "do not change the card's status"; the role
file's step 6 and the S-tier ceremony say otherwise — the third
recorded instance of the class (T-145 and T-093 briefs carried it
before). The lane stopped and recorded rather than resolved: correct.
Also corrected by the lane: the token lint needs no `npm ci`
(CONVENTIONS says why; measured green against an uninstalled
tools/e2e). Both instances are T-155 eval fodder.

## Metrics (ADR-020)

Rework cycles this card: **1** (one REJECTED → one fix pass → one
APPROVED). Suites re-run before green: none red at integration; the
lane's own ledger records its runs. Dispatch → merge elapsed: same
day. Tokens spent, STAMPED from the subagent usage meters (a
live-environment fact, never derivable from the tree):
executor 193,249 + fix pass 234,734 + verifier 160,405 +
re-verification 206,273 ≈ **795k tokens** for the full pipeline arc.
Gate runtime: not derivable here (T-157's slot, pending).

## Dispositions

Stamped: T-158 done. Filed by the lane along the way: nothing new on
main (the verdict's non-blocking notes live in the card's Verdicts
section, one routed item outside fence). No suggestion files added by
this checkpoint.
