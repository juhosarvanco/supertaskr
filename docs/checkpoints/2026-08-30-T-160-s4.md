# Checkpoint: T-160-s4 (2026-08-30, integrator nputer-4e, direct pipeline)

The live planned-card defect the preflight found on its FIRST pass is
fixed on the side the card itself argues: T-059's fence gains
`app-map` (nothing removed), because four of that card's own clauses
point at the line-74 assertion as its content — the title's
one-pin-across-both-engines, the Verification line's app suite, the
pricing criterion beneath it, and the absorbed T-014-s2 which IS the
assertion. Ownership derived from the registry (C-12's own `paths:`),
never from the preflight message.

## The proof, realized not argued

In a lane-free clone: `--task T-059 --preflight` exit 1 (UNCOVERED
CRITERION PATH) at the base, exit 0 after the fix — and the no-other-
card-moved clause MEASURED: 77 dispatch-candidate cards swept at both
refs, one hunk of difference, 76 byte-identical. In situ a finding
SURVIVED — `T-167-s2 crate-index against T-059 crate-index` — and the
lane refused to write a ruling for it, correctly: a real sibling-lane
collision, not a card defect. RE-CHECKED AT THIS CHECKPOINT with
T-167-s2's worktree removed: the finding is gone with the lane that
caused it (`--task T-059 --preflight` is the derive; run it, don't
quote this line). The lane also stamped only `verifying`, refusing a
discharge it had not earned — the closing stamp is this seat's, after
the verification it waited on.

## Also carried

A dated corroboration APPENDED to T-143-s1 (the class carrier)
instead of a fourth sibling card — the metabolism's
search-before-filing clause obeyed; and the port DERIVED from the
lane id (two sibling lanes would both take the default), a discipline
worth copying.

## Suites (exits unpiped, guarded script, after the merge)

parser 315/315 · app 1015/1015 · e2e 320 passed exit 0 · cargo exit 0
· lint:docs 0 (in the merge commit's gate run). Metrics: rework 0;
executor 188,444 tokens (93 uses, 23 min); Gate runtime: the shared
battery's, stamped in the T-167-s2 record beside it. Cold start:
NONE. Drift incidents: NONE.
