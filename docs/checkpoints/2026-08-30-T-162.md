# Checkpoint: T-162 (2026-08-30, integrator nputer-4e, direct pipeline)

The ADR-019 budgets are RE-LANDED — @human's first ruling of the
morning executed in full. Both governing docs compacted on their own
terms, no pin moved and none loosened (the readers never redded, in
the lane or here), warn/fail re-derived by the ADR's own formula, the
addendum written, and both docs-headroom bands read INSIDE for the
first time since the night's six re-breaches. The CONVENTIONS seat
has runway again: `T-156-s1` and the queue behind it unblock here.

## Merge

Parents `d9af25c` and lane tip `7775a0f`, `--no-ff` -> `166ba28`'s
parent (7 paths: both governing docs, docs-scan.mjs budgets, ADR-019
addendum 4, the card, suggestions s1/s2). The landings, derivations on
the addendum: ROADMAP 10,315 -> **9,801** (warn 12,252, fail 14,702);
CONVENTIONS 134,167 -> **131,514** (warn 164,393, fail 197,271),
`ceil(landed × 1.25 / × 1.5)`, the 2026-08-27 rounding. Verified here:
`git cat-file -s` both blobs, the DOC_BUDGETS rows, addendum count.
Outside shipped code -> no independent verifier owed (the partition);
the integration seat verified. The lane's honest refusal stands
recorded: its fence could not write this record, and did not try.

## Verdict corrections, committed-first (166ba28)

Milestone 3's gate text was FALSIFIED between the lane's cut and its
merge — by this morning's own T-025-s5 checkpoint: "no planner turn
has ever run to completion" and "this machine's OAuth token is
revoked" were both false at HEAD. Corrected: the gate paragraph now
states the completed turn and routes the COMPLETION RULING to @human
(one turn, or the full Q1–Q7 walk — a product call). F-03's
restatement of the gate ended (it declared "not restated here" while
restating it). ROADMAP after corrections: 9,765 — under its own new
landing.

## The integrator's slip, and the cascade it bought

The first post-merge battery ran FOUR suites red: parser (4 issues),
app dogfood (same 4), e2e (8 bodies), lint:docs. Root causes, both
mine, both fixed committed-first: (1) `T-025-s2`'s `done` stamp
carried no placement fields — a done card without feature/milestone/
priority/size is a hard parse failure BY DESIGN (the rejected-status
rule's sibling), lighting the board's parse-error badge and cascading
into shell-frame's four bodies; the integrator had verified the stamp
against lint:docs, WHICH IS THE WRONG ORACLE — its frontmatter check
reads "legal status" while the PARSER demands the fields (`87ee86e`).
(2) The `d9af25c` record append left STATE stale against its own
record — the gate said so in words; cured by this checkpoint's STATE
regen. The re-run below is the proof both diagnoses were whole.

## Suites (exits unpiped, guarded script, after both corrections)

Stamped beside the re-run in this record's closing commit: parser,
app, e2e, cargo, lint:docs, health — the battery result line is
appended below when the run completes, never predicted.

## Metrics (ADR-020)

Rework: 0 in-lane. Tokens: executor 312,805 (97 uses, 32 min). Lanes
after: `T-163` (verifying, held for its flip-set landing). The push
REMAINS HELD for `T-143-s4` (dispatches next, onto the freed e2e
seat). Port 1420 untouched.

APPENDED, the re-run (after 87ee86e + f90edfd's STATE): parser 315/315
exit 0 · app 1015/1015 exit 0 · cargo exit 0 · lint:docs exit 0 ·
health exit 3 BY DESIGN, 7 bands inside including BOTH docs-headroom
bands · e2e **319 passed, 1 failed** — session-economics:73 only,
"fences are not disjoint: T-163 app/test against T-112 app-board",
the live-lane class (T-143-s1/T-162-s2's subject), greens at T-163's
landing. Both diagnoses were whole.
