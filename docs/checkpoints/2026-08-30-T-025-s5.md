# Checkpoint: T-025-s5 (2026-08-30, integrator nputer-4e, direct pipeline)

The real smoke waits at real cadence — and the wait was worth it: the
first COMPLETE real planner turn in this project's life ran to its
terminal event under the new deadline, and Milestone 3's last
observed-evidence gap (T-025-s2, open since 2026-08-16) is CLOSED.

## Merge and ceremony

Parents `8e6b18b` (stamps) and lane tip `12795ea`, `--no-ff` ->
`5346d7c` (one frontmatter conflict, my stamp order's doing — resolved
to the union, committed blob verified `status: done`). 3 paths:
agent_runner.rs (+98/-9), the card, suggestion `T-025-s6`.
**Self-integrated under THE SHIPPED PARTITION**: `[app/src-tauri/tests]`
is a bare path outside every slug and KIT_FILES entry -> the
S/outside-shipped-code row -> no verifier owed — and the integration
seat verified by hand: the diff read line by line (one `from_secs(20)`
left, the definition; the smoke the only `REAL_CLI_DEADLINE` caller;
`settle`'s 20 s exception reasoned at the site; all three gates kept).
The executor's report survived fact-checking on every claim.

## The deadline, and why fifteen minutes

Derived, not picked: the runner's own worst-case typed-failure path
(start 30 s + stall 300 s + grace 5 s = 335 s) is the floor; stall is
idle-time and never bounds a healthy turn's wall clock; 900 s ≈ 2.7×
the floor so a panic means "the runner failed to bound itself"; and
the errors are asymmetric — too small destroys the evidence (the
2026-08-30 first run is the worked example), too large costs a
watching human a ^C. Fixtures keep their 20 s alarm.

## T-025-s2 CLOSED — the run @human authorized, to the end

`NPUTER_REAL_CLI=1`, this checkout, after the merge: **Completed,
1 passed, 72.21 s, exit 0 unpiped.** Session `a4c9961e-…`; stage-0
scaffold written (docs/ five + three dirs, CLAUDE.md, AGENTS.md,
.gitignore, git init, nputer.yaml); STATE stamped naming stage 1; the
interview OPENED with a real Q1 — problem-and-person, one concrete
scene, the skip-and-bank-`[?]` escape. ONE Bash call denied by the
allowlist MID-TURN, in band, typed (`denied`, seq 10) — the planner
routed around it and completed: narrow, loud, safe. Measurement
stamped on `T-025-s4`, which it gates. Capture:
docs/research/captures/real-planner-turn-2026-08-30.txt (the
2026-08-19 precedent's shelf).

## The streak broke, and the cause is filed

The stamps push (`8e6b18b`, docs-only) REDDED CI — run #376, ending
the green streak at nine. One body: dispatch-order.spec.ts:308 asserts
the lane-or-no-lane sentence within a FIXED 600-character window, and
`T-025-s5`'s 119-character card path, stamped twice per report line,
overflowed it — on CI only, because only CI has in-flight cards with
no lanes; every local run took the shorter HAS-a-lane arm. The report
itself is honest (the sentence prints unconditionally,
dispatch-order.mjs). Filed as `T-143-s4` (tools/e2e — HELD by T-162's
live lane; dispatches after it lands). The next push's in-flight set
was measured in a CI-shaped clone BEFORE pushing; the result is
stamped beside the push in this record's own log line.

## Suites (exits unpiped, via guarded script, merged main)

cargo 0 · parser 0 · app 0 · boot 0 (`NPUTER_BOOT_PORT=14795`) ·
lint:docs 0 · e2e **1** — 318 passed, 2 lane-caused: both
session-economics bodies, failing on "fences are not disjoint: T-162
tools/e2e against T-157 tools/e2e", the T-143-s1 class answering
correctly while T-162's live lane holds tools/e2e · graph REGENERATED
(+3 symbols in agent_runner.rs, exactly the lane's prediction) and
CURRENT.

## Metrics (ADR-020)

Rework: 0. Tokens: executor 158,486 (64 uses, 13 min). Lanes after:
`T-162`, `T-163` (both building). One real turn spent (~72 s,
@human's authorization). Port 1420 untouched.

APPENDED, same day: the clone measurement ran before the push and
ANSWERED NO — T-162's own row also overflows the window (the phrase
truncates at character 600, mid-word). The push HOLDS at this
checkpoint until T-143-s4 lands behind T-162's merge. Run #376 stays
the streak's honest end; nothing green was claimed that was not seen.
