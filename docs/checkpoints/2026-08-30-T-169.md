# Checkpoint: T-169 (2026-08-30, integrator nputer-4e, blind-verified)

D5 has teeth: the parser derives a per-card assignment verdict (the
MODEL half, sets not representatives, `/` refinement one-directional),
the board surfaces a violation like a parse error with both values
verbatim, and the live census reads ZERO over 227 constrained pairs —
derived, pinned in two executable places, non-vacuous, and proven
capable of failing. The FACTLESS blind verifier's four predictions
about prose-stamp false-positives matched the executor's findings
card for card; its five own mutants all killed; and its two SURVIVORS
became the merge's proven corrections: the @human-prefix hole
(`@human-driven` swallowed a model — regex now matches the word,
pinned, drilled) and the empty-executed-set waves-through (an
unreadable stamp against a binding assignment now VIOLATES, the three
false definition-site sentences rewritten, pinned, drilled;
restoration sha256 73d323ac…f36d). The two-model-stamp judgement
routed to @human as T-169-s2. AT INTEGRATION the drift detector
caught the lane's own confessed placement debt (T-169-s1) as a REAL
undeclared C-08 -> C-09 edge: fixed by MOVING the panel test to a
C-09-declared file, not by declaring convenience — findings back to
four, nine dogfood pins re-derived with reasons, app 1026/1026.
Tokens: executor 262,446 + verifier 180,747.
