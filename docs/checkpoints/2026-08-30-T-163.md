# Checkpoint: T-163 (2026-08-30, integrator nputer-4e, direct pipeline)

Design tokens claim no slug — @human's second ruling of the morning is
landed WHOLE: the merge plus its three fence-blocked complements in
one window, and the board's most colliding card (T-112) drops from
nineteen flip-partners to ONE standing collider. `app-board` and
`app-shell` expand disjoint for the first time in this repository's
life, and the full battery is green with ZERO live lanes.

## Merge and the window

Parents `5f802a0` and lane tip `ce1eea4`, `--no-ff` -> `2ed4460`
(7 paths: C-11's `touch_slugs: []` with the legal-route sentence at
the definition site, select-board's three re-derived bodies, T-112's
dated flip-pair correction, the card, three suggestion cards). The
lane's report survived verification: the spelling argument (an absent
slug cannot be named — the property is structural where a
"never-fenced" slug would be a promise kept by nothing), the
before/after of the ledger row, and the honest stop at criterion 1
(the pin's positive control loses its subject — the card's own
criterion was WRONG, and the lane said so instead of reaching outside
its fence).

**The three complements, performed in the merge window** — forced
there by the method's own rule (a lane must be cut from a known-green
base, and the window between this merge and its complements is red by
construction), each with the executor's diagnosis as the map and the
select-board rewrite as the model, every live half now stating the
RULED NEGATIVE and every mechanism moved onto a synthetic registry
(one component, two slugs) so nothing is weakened:

1. **s1** — ARCHITECTURE.md's slug block re-derived; the
   two-slugs-share-a-component sentence replaced by the ruling with
   its date and route.
2. **s3** — both parser fence bodies: the doubly-claimed census is
   now the negative that keeps the ruling ruled
   (`doubleClaimed toEqual []`), and PIN TWO (a) pins the live pair
   DISJOINT while the shared-component catch runs against C-70
   (a spread of the real C-11 under two synthetic slugs).
3. **s2** — both brief.spec controls: the two-sided join asserted at
   `[]` with the ledger's own negative sentence pinned
   ("no component is claimed by two slugs today", "both expand
   through" absent), and the join proven against a populated
   synthetic map.

All three suggestion cards DISCHARGED-NOT-DECLINED with the reason
and this record named. T-163 `done`, verified by the integration
seat; T-112's correction stamp (27 -> 2 at the lane's ref, ONE
standing) travels on its card.

## Suites (exits unpiped, guarded script, zero lanes)

parser 315/315 exit 0 · app 1015/1015 exit 0 · e2e **320/320 exit
0** — the first fully green e2e on this machine since the morning's
first dispatch; both session-economics bodies greened the moment the
last lane closed, exactly as the T-143-s1 class predicts · cargo
exit 0 · lint:docs exit 0 · health exit 3 BY DESIGN (7 inside, 0
drifting, 0 BREACHED) · graph REGENERATED and CURRENT · capabilities
CURRENT (no e2e spec name moved).

## Consequences now standing

T-112 is dispatchable per @human's ruling with ONE collider
(T-031-s1, to be ruled or absorbed at dispatch). The e2e seat is
free; `T-156-s1`, `T-160-s4`, `T-162-s2` queue on it. The board
holds no building cards but T-135's standing half; every lane of the
day is landed.

## Metrics (ADR-020)

Rework: 0 in-lane; the window's three complements were the priced
alternative to three illegal lanes. Tokens: executor 271,573 (156
uses, 29 min). Lanes after: NONE. Port 1420 untouched.
