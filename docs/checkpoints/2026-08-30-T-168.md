# Checkpoint: T-168 (2026-08-30, integrator nputer-4e, direct pipeline)

The customization-form decision brief landed and was RULED THE SAME
HOUR — all nine questions, @human, one-word answers on a widget with
the brief open beside it. The feature @human directed this morning
went directive → room → sitting → cards → lane → brief → ruling in
one day, with no UI drawn before the form was chosen, exactly as the
FORM-FIRST condition ordered.

## Merge and evidence

Parents (T-156-s1's stamps) and lane tip, `--no-ff`, auto-clean.
4 paths: docs/research/customization-ux-benchmark.md (25+ tools,
four schools, source URL beside every claim, limits stated),
docs/rooms/customization-form.md (the brief), the card, suggestion
T-168-s1 (the session-economics class refined AGAIN: the disjointness
set is built from live worktrees — measured both directions). Spot
verification here: sources cited per claim, no mockups anywhere, the
nine questions each one-word answerable — the ruling proves the form
worked.

## The rulings (recorded in the room's RESOLUTION, @human 2026-08-30)

Q1 HYBRID, asymmetric — files ARE the system; the app reads,
explains, proposes, routes, never authors. Q2 PROPOSE. Q3 YES,
imports only, each owing a card. Q4 YES — a `review:`-class field
joins D3's stampable set. Q5 DRIFT → CARD with per-skill interim —
@human's own reframing: harmony is the birthright; later
disagreement is drift, surfaced never silently resolved. Q6 PER
SKILL. Q7 EXPLAIN SURFACE FIRST. Q8 EVERY CHANGE OWES A CARD.
Q9 HOST. UI cards are cut at a sitting under the version-planning
room's standing rule — the charter is a vision, not a queue.

## Suites (exits unpiped, at this merge)

lint:docs 0 · e2e **320 passed** exit 0 · health `--readings` (the
new ritual's FIRST integration use): 8 inside, 0 drifting, 2 unread,
4 UNKEPT, exit 3 by design — `suite/e2e-seconds` read INSIDE from
the captured battery output. parser/app/cargo covered by the
flanking batteries (T-156-s1's and T-167's records).

## Metrics (ADR-020)

Rework: 0. Tokens: executor 297,860 (140 uses, 35 min). Cold start:
NONE. Drift incidents: NONE this checkpoint.
