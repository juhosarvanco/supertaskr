# Checkpoint: T-140-s1 (2026-08-30, integrator nputer-4e, blind-verified)

THE WALL IS REMOVED, NOT POSTPONED. The map's resting payload is the
component rollup — flat by construction, verifier-measured at 464→468
bytes over a 14× file spread across FOUR adversarial tree shapes
including the no-registry worst case — and file detail is a PULL that
refused all 22 of the verifier's hostile targets by name with a
planted canary appearing in no answer. An over-budget graph now
degrades the DRILL, never the map; and after the corrections, the
pane tells the truth about every degradation it inherits.

## Merge, verdict, corrections

Lane tip `f586f82`, `--no-ff`; 22 paths (+3,894). Verdict: APPROVED
WITH ASSIGNED CORRECTIONS — the verifier isolated the lane's graph
cost with its own base-worktree measurement (+30,612 bytes, judged
honest: ~9.5 bytes per added line, nothing stopped being indexed),
re-measured flatness itself, and verified ADR-015's boundary unmoved.
Three corrections performed at merge, committed-first: (1) the sliced
graph carries the rollup's `truncatedSymbols`, so the oversize mode
renders the truncation note instead of "no symbols declared"; (2) the
pull's 500-file clip is SPOKEN — `pulledTruncated {shown,total}` on
the derived component, the "…and N more" row in the panel; (3) the
load-bearing disclosure on the card: the Rust reader's answer is now
what users see at rest while the live-tree pins exercise only
`deriveDeclared` — T-059 (blocked_by T-033) is now load-bearing.

## The integration window's own finds

The verifier's predicted two-file D2 was PRE-EMPTED: arch_cmd.rs and
map-rollup.test.ts claimed (C-05, C-12) in the regen's own commit —
findings 4, unmapped 0. And the window found a hazard of its own: the
marketing session's worktree lives INSIDE the repo
(.claude/worktrees/), and the first regen indexed a full second copy
of the tree — 391 files, EVERY symbol degraded away (the T-153-s3
class, new member). .nputerignore now excludes /.claude/ and /bin/,
ROOT-ANCHORED after the first spelling ate two real bin/ source files
(gitignore depth semantics — owned, reasoned at the line). Eleven
dogfood pins re-derived with reasons.

## Suites (exits unpiped, guarded script with build echoes)

parser build 0 · parser 336/336 exit 0 · app build 0 · app suite
exit 0 · e2e exit 0 · cargo exit 0 · graph REGENERATED and CURRENT in
the sealing commit: **198 files, 2,095 symbols, 1,037,788 bytes —
2,212 left, alarm printing** — and for the first time since the
degradation began, the number is survivable: the resting map no
longer reads what the emitter drops. T-140-s4 (the limit ruling,
@human's) and T-140-s6 (search over unopened files) carry what
remains; T-140-s5/s7 discharged by main's own earlier fixes and the
window's registry claims.

## Metrics (ADR-020)

Rework: 0 in-lane; 3 verdict corrections + the window's regen fixes
at integration. Tokens: executor 436,129 (217 uses, 52 min — the
day's largest) + verifier 251,240 (115 uses, 22 min). Cold start:
NONE. Drift incidents: ONE — the nested-worktree double-index, caught
before commit, immunized in the same window (denominator: 1 merge).
Lanes after: NONE. Port 1420 untouched.
