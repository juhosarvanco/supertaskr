# Checkpoint: T-155 (2026-08-29, integrator nputer-4e, direct pipeline)

The method can now red its own degradation before it ships: ten canned
tasks under tools/method-evals/ (six model-free, four model-in-loop),
a founding review-claims corpus where every figure is an EXECUTABLE
derivation, and a version bump owes a fourth obligation — run the
evals, print the block, name the runner.

## Merge

Parents `1ba7b4d` (main, the T-156 checkpoint) and lane tip `2b0ee6b`,
`--no-ff` -> `52d9e62`. 33 paths: the suite (evals, fixtures, lib,
run.mjs — zero dependencies, answers against a bare checkout), the
CONVENTIONS METHOD EVAL GATE bullet + fourth bump obligation, the
card, seven suggestion cards (s1 CI wiring, s2 lint coverage, s3
ARCHITECTURE layout, s4 the ROADMAP contradiction, s5 the founding
instances, s6 MIL shape-scoring, s7 docs-gate linkability). Verdict:
**APPROVED WITH ONE ASSIGNED CORRECTION**, performed here: MF-04's
header now names the FOURTH silent shape (a bare, unprefixed
reference to a nonexistent method-root file is never examined — the
verifier measured exit 0 over a planted `lane-protocolz.md`), honest
about the hole instead of implying coverage; widening the predicate
is design work routed to the s6 family. Model-free suite exit 0 and
`--selftest --set all` 10/10 POSITIVE CONTROL re-run after the edit.

Two things the verifier settled beyond the card: the e2e base count
was definitively 258 (259 unproducible under any env spelling — a
handoff figure, never on a card), and `NPUTER_E2E_PORT` is the only
port variable the suite reads.

## Gates

- DOCS GATE: FIRES (exit 1) — and it first refused my ambiguous cwd
  outright, the T-091-s2 class made mechanical, catching this
  sitting's sixth cwd slip. All owed suites below.
- METHOD EVAL GATE (new, its first checkpoint): 0 of 33 paths under
  method/** — not owed; run anyway, exit 0.
- GRAPH REGEN: 0 indexed suffixes; asked LAST after this record's
  final write — CURRENT, 1,021,562 bytes, exit 0.
- BOOT GATE: not owed. capabilities:check CURRENT (21,886).
- lint:docs 0 · lint:tokens 0.

## Suites (counts and exits, unpiped)

cargo exit 0 (18 result lines, all ok) · parser 314/314 exit 0 · app
1013/1013 exit 0 · e2e **280/280** exit 0 (`NPUTER_E2E_PORT=14640`,
lsof zero rows first) · method evals: model-free exit 0, selftest
10/10 exit 0.

## Metrics (ADR-020)

Rework cycles: **0**. Tokens, stamped from the meters: executor
333,115 + verifier 299,780 ≈ **633k** (size L). Arc series: 484 /
573 / 795 / 477(140) / 530 / 513 / 633k. `npm run health` at this
checkpoint: 6 inside, 1 drifting (ROADMAP 4.53%), 0 breached, 3
unread, 4 unkept — unchanged from T-156's reading after its
remedies.

## Routed, standing

The MIL acceptance functions have never met a real model (the
executor's least-confident line, the verifier's s6 measurements) —
the first real calibration run turns every declared floor into a
figure, and that run belongs with @human's T-025-s2 credentials
moment. T-155-s4 (the executor-brief ROADMAP contradiction) joins
T-159's review-reconciliation text with the brief-format flaw from
this sitting's verifier dispatches.

## Environment

Lanes after this checkpoint: NONE — the board is briefly all-merged.
T-155's worktree REMOVED, branch kept. Next: T-153-s5 dispatches
(tools/e2e freed here), then T-153-s6 (CONVENTIONS freed here too);
main pushes when they land, aiming at the repository's first green
CI run. Port 1420 untouched; `z` unmoved.
