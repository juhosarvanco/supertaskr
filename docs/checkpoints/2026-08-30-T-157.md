# Checkpoint: T-157 (2026-08-30, integrator nputer-4e, direct pipeline)

Session economics enters the seats: the brief now prints a derived
seat-STRENGTH recommendation below the contract rows (never a model —
ADR-003/D5; outranked by `builder:` and the role file, and it says so
every run), the checkpoint TEMPLATE's metrics slot becomes a stated
three-line obligation, and the first card through the T-160 preflight
ritual dispatched on a mechanically clean card.

## Merge and ceremony

Parents `88890d0` (main) and lane tip `f19cc2b`, `--no-ff` ->
`0ed49a9`. 8 paths: session-economics.mjs (+ its 10-body spec),
brief.mjs's advisory block, TEMPLATE.md, the card, three suggestion
cards (s1 the method-side hygiene text, s2 the cost-trend adder with
its records-are-not-inputs constraint, s3 EARS-shape in the
preflight — filed against this card's own third criterion, which the
tool it built flagged first). **SELF-INTEGRATED, no verifier seat**:
TASK-FORMAT's rule puts a tooling/docs advisory diff on the
self-integrating row, the deliverable is a non-binding printed row
plus template prose, and the integration seat verified instead —
recorded here, per ceremony-scales-with-blast-radius and the
T-127-s1 precedent.

## The integration seat's own verification

- Full battery re-run: e2e **313/313** (base 303 + 10), app
  1015/1015, parser 314/314, lint:docs 0, capabilities REGENERATED
  (24,849, CURRENT).
- ONE OWN MUTANT, not a replay: the tie-break flipped (`>` -> `>=`,
  tie-to-KNOW) — **SURVIVED 10/10**, and the survival is DISPOSED BY
  ARGUMENT, not chased: with exactly three signals a tie is
  unreachable through the public surface, the spec's own comment
  derives this, and the clause exists for a future fourth signal.
  T-091-s6's rule — an equivalent/unreachable mutant is disposed in
  writing, or the next drill chases it again. Restoration
  sha256-proved (66d542f493d1).
- Calibration denominator corroborated: 327 cards on board = the
  executor's 324 + this lane's three suggestions.

## Card corrections observed, not performed

The executor found the card cites D5 for `builder:` authority where
the ruling is D3 (D5 is deliberately HELD, and load-bearing for the
no-model half instead) — the tool cites correctly; the card's spec
text was left as the author's word, per the executor's own restraint.
The know-vs-try "heuristic" exists nowhere but the card; the
mechanical majority rule is the lane's reading, measured over the
board (67 KNOW / 257 TRY at 88890d0) with re-measure instructions in
the module header.

## Gates (exits unpiped)

GRAPH REGEN: fires by trigger, asked — CURRENT (tools-only, no-op).
DOCS GATE: fires, three suites named, all green above. BOOT/METHOD
EVAL: not owed. `docs-scan` census: 23 readers, unchanged — the new
module cites rulings by id, never by docs path, so no file was
enrolled as a reader by accident.

## Metrics (ADR-020, per the slot this checkpoint just formalized)

Rework cycles: 0. Tokens, read 2026-08-30 on Mac.lan from the
meters: executor **260,223** (117 tool uses, 34 min); verifier: none
(self-integrated, above); integrator verification: this record's own
session share, not separately meterable — `not derivable here`
because the coordinating session's meter spans many arcs. Gate
runtime: e2e 3.0m stamped from the runner's own summary.

Lanes after: NONE. Worktree REMOVED, branch kept. Next per the ruled
order: **T-159** (the metabolism release — preflight first), then
the first standing triage under its rules. Port 1420 untouched; `z`
unmoved. Main pushes from this checkpoint.
