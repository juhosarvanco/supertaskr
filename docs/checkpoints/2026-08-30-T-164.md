# Checkpoint: T-164 (2026-08-30, integrator nputer-4e, direct pipeline)

@human's launcher exists: `bin/app-dev.mjs`, one command, no
arguments — detaches the app worktree at local main, install-if-needed
(derived, stated where it decides) + always-rebuilds lib/parser,
installs app if needed, starts tauri dev; stops LOUDLY at the first
failure with the step named; REFUSES a lane, the integration checkout,
and a missing target, each with the reason and the fix; port 1420
never read, probed, or bound. `.mjs` deliberately — outside the
indexer's language allowlist, so the launcher spends none of the graph
budget. Proven by four dry-run captures against a fixture repo plus a
live fixture run demonstrating the loud stop (STEP 3 FAILED, no step
after ran). Env var: NPUTER_APP_WORKTREE. Two honest routings:
T-164-s1 (the already-running question needs a ruling on whether
STATE's one permitted lsof read counts as probing) and T-164-s2
(nothing outside bin/ says the launcher exists; the CONVENTIONS
listing owes its LOCAL_ONLY entry in the same commit). Merged
--no-ff, stamped done, verified by the integration seat (bare-path
fence, no shipped byte). Tokens: executor 142,865.
