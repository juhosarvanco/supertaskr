# Checkpoint: T-147 (2026-08-30, integrator nputer-4e, direct pipeline)

The shipped partition is STATED — the debt the method names for
itself and this project never paid. Beside the slug map, in slugs:
every registry slug ships; a bare `method/` path ships when it
REACHES a `KIT_FILES` entry (over-firing on purpose — an "equals"
reading would let `[method/roles/]` self-integrate while it can
rewrite planner.md); everything else is not shipped, with tools/e2e's
yes-code/no-ship case named. Both derivation commands travel with the
statement; no set is transcribed. The ceremony row is now MECHANICAL.

## Merge and ceremony

Parents `aa8348d` (dispatch) and lane tip `6934ff8`, `--no-ff` ->
`0c77a84`. 4 paths: CONVENTIONS (+1,785 — headroom 3,761, WARN not
crossed per the card's own criterion), the card, two suggestion
cards (s1 the brief's derivable-row line, s2 the beside-a-map
wording upstream). **Self-integrated under the rule this very card
landed**: `[docs/CONVENTIONS.md]` is neither a slug nor a KIT_FILES
path -> the S/outside-shipped-code row -> no verifier, executor
self-integrates — and the integration seat verified: nine
CONVENTIONS readers derived and green (84/84 across the three key
spec files re-run here; cargo/parser/app/e2e green in the lane).

## Corrections to the dispatcher's own claims, stamped

The brief claimed "the adapters are cmp-pinned twins since T-159" —
FALSE. The T-159 lane MEASURED agreement once; nothing pins it, and
T-144 (planned) exists precisely for that gap. Both halves of
T-145's safety net remain absent until T-144 lands — a fact the
brief's over-claim could have buried, caught by the executor reading
the tree instead of the brief. Also corrected: "three CONVENTIONS
readers" is nine; the card's "thirteen components" is fifteen files,
eight distinct slugs, fourteen KIT_FILES entries — all re-derived.

## The seat's runway, for @human

CONVENTIONS now holds 3,761 bytes of warn headroom (2.7% — the band
reads breached). The CONVENTIONS train is OUT OF RUNWAY: T-156-s1's
checkpoint-carry edit and any further sentence land within a
whisker of the warn. The queued decision (with ROADMAP's) is one
ADR-019 re-landing pass over both budgets; until ruled, no further
CONVENTIONS lane dispatches from this seat.

## Suites (exits unpiped)

Lane: cargo 525/0/4 ×2, e2e 313 ×2, app 1015, parser 314, lint:docs
0, index --check CURRENT. Integration: lint:docs 0,
workflow-parity + brief + docs-input-gate **84/84**, health read
(ROADMAP the one breached band, priced). No drills — no assertion
was added, and the record says so plainly rather than dressing
positive controls as mutants (the executor's own honest line).

## Metrics (ADR-020)

Rework: 0. Tokens: executor 181,885 (91 uses, 21 min) — the
cheapest full card of the night; the partition was one derivation
plus judgement. Lanes after: NONE. Worktree REMOVED, branch kept.
Remaining ruled order: `T-143` (tools/e2e, disjoint from the
runway problem) — then the morning report. `T-156-s1` HELD on the
CONVENTIONS runway, routed to the re-landing decision.
