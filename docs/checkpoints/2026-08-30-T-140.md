# Checkpoint: T-140 (2026-08-30, integrator nputer-4e, direct pipeline)

The file ceiling is measured, printed, and honest: `index --check` now
derives the undroppable floor on every run (at this landing: 213,914
bytes, 1,132 B/file, ~918 files at this tree's density), and a graph
too large to deliver tells the map the truth instead of blaming an
index that ran. No budget VALUE moved — every T-151-shaped pull was
refused in writing, and T-151's own arithmetic note is re-derived
(8,575 legal bytes ≈ seven files at the landed density).

## Merge

Parents `eecc83e` (main) and lane tip `229b3f7`, `--no-ff` ->
`0d63006`. 12 paths: crate-index (floor_len, floor_line, the relation
body), docs_watch's criterion-4 ruling at the definition site, the
App/MapView oversize fix, the card, three suggestion cards (s1 the
shape change reserved for @human, s2 the filename-vs-regex class, s3
the skip-reason mismatch the verifier found). Verdict: **APPROVED
WITH FOUR ASSIGNED CORRECTIONS**, all performed here:

1. The projection's ARITHMETIC pinned (the verifier's surviving
   mutant halved the printed ceiling and shipped green — the printed
   figures are now re-derived inside the body; drill: green).
2. The SHELL wiring pinned (the second surviving mutant unwired
   App.tsx's graphSkip and shipped 1014 green — a new App-mounted
   body in map-shell-dom drives the skip through the store; the
   mutant now reds it alone, restoration sha256-proved; the body
   also caught this file's payload helper silently swallowing its
   second argument — the transpile-without-typecheck class).
3. The drill record's SHAPE SIX claim replaced by the verifier's
   measured unique kill (files.truncate(24), 1 of 525), keeping the
   honest half: edges.clear() still cannot red it, which is why it
   stays T-140-s1's discriminator.
4. The stale 802/1,000 headline retracted at its site (title and
   opening), the filing-time table kept as record.

## Integration reconciliations stamped here

- GRAPH REGEN performed (owed by the lane's .rs): 1,022,964 bytes,
  98.4%, CURRENT — and the regen surfaced the lane's OWN
  MapView->docs-model `SkipReason` import as a sixth C-12->C-10 edge;
  the dogfood row updated 5 -> 6 with the citation (the lane's
  battery correctly ran pre-regen).
- The verifier's density sweep gives T-140-s1's @human decision its
  number: ceiling ~2,846 to ~406 across plausible import densities
  (~7x) while stable to 0.5% across an 8x size range — the linearity
  is the product fact, the constant is this repo's density.

## Suites (exits unpiped, at the combined tip with T-153-s9 merged)

cargo 18 result-lines all ok (incl. the new arithmetic pin) · parser
314/314 · app **1015/1015** · e2e **283/283** · lint:tokens 0 ·
lint:docs 0 · boot: run by the lane (exit 0). The battery interlude
is stamped honestly: between this merge and this record, main
carried parity reds from the T-153-s13 guard (three copies of one
command); the alignment landed with the s9 checkpoint below and the
battery above is the aligned, green one.

## Metrics (ADR-020)

Rework cycles: 0. Tokens: executor 353,393 + verifier 302,236 ≈
**656k** (size L). T-140-s1 (the payload shape change) and T-151
(the budget value) both sit at @human's line, now with measured
numbers attached.
