# Checkpoint: T-092 (2026-08-27, architect claude-opus-5 as integrator)

## Merge

`162b5ed`, parents `5887cd4` and `216a5a2`, `--no-ff`. Forecast
`git merge-tree --write-tree 5887cd4 task/T-092-lane`, **exit 0 read
from `$?` before the substitution**; **10 paths** — `kit.rs`,
`docs/CONVENTIONS.md`, the card and seven `T-092-s*` findings. Nothing
outside the fence `[docs/CONVENTIONS.md, app-agent]`. Only lane live;
disjointness vacuous and stated as such.

## Gates

- **GRAPH REGEN — FIRED, and the identical-figures trap fired with it
  for the FIFTH recorded time.** `index --check` exit 1 before, exit 0
  after. The regenerated graph is **byte-for-byte the same size**
  (1,020,023), same 189 files, same 2,152 symbols, same 2,111 edges.
  The only movement: `app/src-tauri/src/agent/kit.rs`, **loc 514 → 562**.
  Every published figure matched while the tree was stale.
- **BOOT GATE — owed and RUN by both lane and verifier**, ports read at
  zero rows on both stacks before binding, exit 0, both `[nputer]` lines.
- **DOCS GATE — FIRES**, four docs paths as code inputs, all four owed
  suites run.
- **ADR-019 BUDGET — HOLDS, and is nearly spent.** `docs/CONVENTIONS.md`
  **95,644 → 107,048 (+11,404)** against warn **107,967** — **919 bytes
  left, 92.5% of the seat's headroom consumed by one card** after two
  compression passes. `lint:docs` exit 0 and does not WARN.

## Suites

cargo **518/0** exit 0 · parser **314/314** exit 0 · app **1013/1013**
exit 0 · e2e **233/233** exit 0 (port 16053, `lsof` zero rows before the
bind) · `lint:docs` 0 · `lint:tokens` 0.

## Board

`T-092` → `done`, `review: same-model`. Seven findings arrive with it,
`T-092-s1` through `s7`. Both CONVENTIONS floor-droppers named by
ADR-019's addendum are now discharged; **neither dropped the floor** —
T-093 added 8,926 bytes and T-092 added 11,404.

## Environment

Read 2026-08-27 on Mac.lan: port 1420 held by `node` pid 19746, the
human's app — `lsof` only throughout, by lane, verifier and integrator.
The verifier drilled at `/private/tmp/v092-1240c3f2`, stem derived from
its session id; the lane at `/private/tmp/t092-drill`, stem derived from
its card. **Both derived rather than fixed — the card's own remedy,
applied while the card was being built.** Untracked `z` left alone.

## What the brief got wrong

Mine said *"the three unnumbered **shas**"*. They are three unnumbered
**shapes**; the card's own heading says so. The verifier declined to
inherit the noun and said so.

## Dispositions

**`T-092-s4` is the finding that outlives this card.** After
`git checkout <commit> -- <path>`, `git diff -- <path>` reports **0
bytes on the wrong file** — so one of POISON DRILL's two prescribed
restoration proofs **passes on a failed restore**, and the belief that
committing first makes both correct by construction is falsified. Every
drill run under that clause has been relying on the other proof. It
belongs at the POISON DRILL seat and should be triaged high.

**`T-092-s2` should be triaged AHEAD of the next CONVENTIONS card** —
919 bytes is not headroom for anything.

`T-092-s7` is the lane auditing its own method: the notes derived their
gate list from the banned pre-merge two-dot pair while naming it the
prescribed one, and reached the right answer only because main never
moved. `s3`, `s5`, `s6` are text-level and non-blocking. Worktree
removed and pruned.
