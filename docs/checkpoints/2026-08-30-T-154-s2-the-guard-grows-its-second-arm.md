# Checkpoint: T-154-s2 landed — the guard grows its second arm, and its first correction was measured on this very integration

Date: 2026-08-30. Seat: integrator. Scope: T-154-s2 merge and close;
the e2e battery whole again at 332; T-112 executor out, verifier
dispatched; the graph-budget klaxon for T-112's merge.

## T-154-s2: APPROVED WITH ASSIGNED CORRECTIONS, landed

@human's ruling (lane-less seats' writes IN SCOPE) is now mechanical:
a write from a checkout that is not a lane to a path some live lane's
manifest reserves is refused, carve-outs as stated criteria
(docs/tasks/ unfenceable, a card's own file, the integration seat's
docs/STATE.md + docs/checkpoints/), every refusal resting on a
positive readable reservation, every uncertainty on the lane-less arm
an ALLOW. Zero-subprocess walk of git's worktree administration:
verifier re-measured 0.152ms against `git worktree list`'s 9.404ms
(62×; executor's band 47–62×), runner ~37–41ms end to end. 37 spec
bodies; executor's 19 mutants + verifier's 15 (14 killed
count-matched, 1 proved EQUIVALENT with its future-body condition
written down). Fence CLEAN. Live-observation gap RULED SUFFICIENT
from the card's own words — and then observed anyway, both ways: the
verifier drove the real runner to an exit-2 refusal naming T-112's
lane, and the integration's own correction edits were the new arm's
first live ALLOWs from a lane-less seat.

Three corrections, all disclosure, all performed at merge:

1. **The mid-integration criterion closes before the lane does** —
   git drops its marker at the merge COMMIT (a clean merge never
   writes one) while rule 6 keeps the worktree until after the
   checkpoint, so post-merge verdict corrections into the just-merged
   fence are refused for that window. Now hook limit 6 + a
   CONVENTIONS paragraph + a bullet on T-154-s4 (whose ruling must
   answer it). **Self-demonstrating: this very integration removed
   the worktree BEFORE the correction edits, on the verdict's own
   route — the first integration shaped by the guard it was
   landing.**
2. **The case escape** — on this case-insensitive volume
   `DOCS/ROADMAP.md` is allowed where `docs/ROADMAP.md` is refused
   and both write the same file; new to the lane-less arm because
   allow-by-default converts case into a hole where the lane arm's
   block-by-default only tightens. Declared as limit 7; the close is
   a routed question, not a case-insensitive compare that would
   over-refuse elsewhere.
3. **"BYTE-IDENTICAL" retracted** — `readManifest` now requires
   `excluded`, one verdict differs for a manifest no live writer ever
   produced. Stated in the hook header and at both card anchors (the
   second anchor's grep was defeated by a line break and the verdict
   append briefly said it did not exist — corrected in place, said
   so).

Board: T-154-s2 done (all four fields claude-opus-5@subagent,
review: independent honoured — builder and verifier different seats).
New on the board from the lane: T-154-s3 (rule 5 prose, next method
release), T-154-s4 (mid-merge carve-out ruling, now carrying the
window bullet). Verifier observations left for triage: the lane-less
arm honours a sibling manifest at face value (no re-expand check);
the session-economics attribution clears when the CARD leaves
building, not when the worktree goes — measured true today: with the
card done and the worktree gone, **e2e 332 passed, exit 0** — the
battery is whole again. typecheck/lint:tokens/lint:docs 0, parser
336/336, graph CURRENT at 2293 edges (this diff moves no indexed
file).

## T-112: executor out, blind verifier dispatched

Lane tip 96e3886, status verifying in-lane. Headlines for its
integration, from the executor's own gate derivation:

- **GRAPH KLAXON: at T-112's merge the fresh index is 1,038,884 of
  1,040,000 bytes — 1,116 left — and stats.truncated_files moves
  2→4, with docs_watch.rs dropping 55 symbols → 0.** T-140-s4's
  hazard arrives at that merge. Read before regenerating; the ruling
  on the limit is @human's open item.
- Census does not move (13/17/13 both-ends intersection), acl_pin
  0-file diff at 92 grants. The registration of the new assembler is
  app-shell and OUTSIDE the fence → routed T-112-s1 (defective-card
  class, fence not widened — same disposition as T-110-s1).
- 17/17 mutants killed + one measured survivor routed (T-112-s4:
  C-18 declares no test path). Suggestions T-112-s1..s4 on the board.
- Inherited red, attributed not absorbed: capabilities:check exit 1
  is T-153-s8's by name.
- All suites exit 0 at the lane tip, including e2e 321 — the
  live-lane class did not fire on it.

Blind factless verifier dispatched at this record (two-phase
attack-set protocol, contract before facts).

## The incident-class ledger

The stamp scripts this session both stopped at their asserts and
nothing was committed on top of a dead script — the 18d8166 rule
(an edit script's success is a gate) held on its first two outings.
