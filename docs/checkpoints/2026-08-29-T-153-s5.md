# Checkpoint: T-153-s5 (2026-08-29, integrator nputer-4e, direct pipeline)

The clock-restore guard now asserts what the toolchain actually
grants: libuv's truncation quantum plus two COMPUTED ULPs, read back
at nanosecond precision and stamped on every run — proven green on
Linux across three CI cycles and against mutants from both sides
(1431 ns green / 1669 ns red is the discriminating pair). Two of
main's four carded Linux reds die at this merge.

## Merge

Parents `268f544` (main) and lane tip `5690f57`, `--no-ff` ->
`0660547`. 6 paths: token-scan.spec.ts (the bound, `ulpOf`,
`expectClocksRestored` with permanent read-back stamps), the card,
and four suggestion cards (s7 the clock-sentence rider for T-111-s10,
s8 the census-keeper gap, s9 the PR-checkout class, s10 the bound's
unkilled elements — filed by the verifier). Verdict: **APPROVED WITH
THREE ASSIGNED CORRECTIONS**, all prose, all performed here:

1. **The axis is the libuv VERSION, not the platform** — v1.51.0
   truncates under one `#if` naming `__APPLE__` and `__linux__`
   together; v1.52.0 deletes the hack; CI ships 1.51.0, the measuring
   Mac 1.52.0, so the first samples differed in BOTH variables and
   read as a platform split. Corrected in the spec header, the card's
   MECHANISM, and T-153-s7's framing ("Linux half" -> version half).
   The stamped `process.versions.uv` is now the load-bearing line: a
   runner-image bump moves the quantum with no platform changing.
2. **"Strictly tighter" retracted** — only the INSTRUMENT tightened
   (ns read-back, permanent stamps); the acceptance band is ~6x wider
   than the old exact equality (measured: 173–1296 ns deltas pass new,
   failed old), deliberately, because the old equality was green only
   where one toolchain round-trips exactly.
3. Sample figures re-stamped: 24 Linux observations over three runs
   (third: 33266566174), max |delta| 1016 unchanged.

## Gates and suites (exits unpiped)

e2e **281/281** exit 0 (`NPUTER_E2E_PORT=14690`) after the
corrections · parser 314/314 exit 0 · capabilities CURRENT (21,992) ·
lint:docs exit 0 · GRAPH REGEN not owed (no indexed suffixes; asked
LAST after this record's final write — CURRENT, 1,021,562 bytes,
exit 0) · BOOT GATE not owed.

## CI evidence (the lane's, verifier-confirmed first-hand)

Both guard bodies PASS on ubuntu in all three lane runs (33264083542,
33265405734, 33266566174). The runs' other reds are fully classified:
2 are T-153-s6's xargs pair; 29 are T-153-s9's PR-checkout class
(no local `main` at a pull_request ref — 28 quote the fatal, the 29th
is the same call site one layer up), all green at the same bodies on
main's push run. Draft PR #3 stands as the lane's CI record and
closes when main pushes.

## Seat notes for the eval corpus

- The verifier could not falsify the 1.5x margin and re-derived it
  from libuv source + IEEE-754 rather than from the samples — the
  bound is a closed-form worst case the distribution sits inside, not
  an extrapolation. Its correction 1 makes the residual risk
  version-shaped, which the per-run uv stamp watches.
- Both seats disclosed the same brief-format weakness (the executor
  report travels in the same message as the phase-1 instruction, so
  blindness is discipline, not guarantee) — second and third
  sightings; T-159 owes the fix (a two-message dispatch, or the
  report handed only on request).
- The executor's cycle-2 self-correction (a "law" from cycle 1
  falsified three ways on the same runner) is the derive-never-quote
  rule catching its own author mid-flight — corpus material.

## Metrics (ADR-020)

Rework cycles: **0**. Tokens: executor 328,720 + verifier 243,722 ≈
**572k**. Lanes after this checkpoint: NONE — `T-153-s6` dispatches
next (the fence frees here), then MAIN PUSHES for the green-run
attempt. T-153-s5's worktree REMOVED, branch kept; the verifier's
/private/tmp drill worktree removed by its own hand. Port 1420
untouched; `z` unmoved.
