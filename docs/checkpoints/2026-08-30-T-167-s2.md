# Checkpoint: T-167-s2 (2026-08-30, integrator nputer-4e, direct pipeline)

THE GRAPH HEADROOM ALARM IS LIVE ON MAIN — and it fired on its first
breath: `!! GRAPH HEADROOM ALARM - 6865 bytes left, under the
14914-byte tripwire`, printed unindented above the stat lines on a
CURRENT graph at exit 0, on both the CURRENT and STALE paths. The
ceiling's approach can no longer hide inside a green gate's indented
statistics.

## Merge and evidence

Lane tip `0815bb9`, `--no-ff`, clean. 4 paths: check.rs (+227 loc,
the alarm and its threshold), the card, suggestions T-167-s5/s6.
Verified here: the threshold's derivation travels in its own doc
comment — `WARN_HEADROOM_BYTES = 14_914`, the mean of ALL 61
single-commit growths of graph.json in this repository's history
(median 5,230, max 241,980), re-derived at the lane's base; the exit
code deliberately untouched with the reason at the definition site
(exit 1 means STALE; a low-headroom graph is current); the
health-band's parser CHECKED against hijack by feeding it the new
output (`{"value":6865}`, the band still reads). Drill: 6 mutants, 6
one-sided kills, restorations sha256-proved in both trees. The lane
disclosed a stray placeholder written outside every repository and
deleted — nothing tracked touched.

## The number to watch

This lane itself spent 530 bytes; T-025-s6's (verifying) spends
+2,172 more — the fresh index there reads 99.5%, 5,223 left. The
alarm will print on every gate run until T-140-s1's rollup lands,
WHICH IS THE POINT. The fix chain stands: T-135 Half B ->
T-140-s1; T-167-s5 (alarm names what YOUR tree just spent) and
T-167-s6 (the threshold has no pin across packages) queue for the
next sitting.

## Suites (exits unpiped, shared battery after both merges)

parser 315/315 · app 1015/1015 · e2e **320 passed** exit 0 · cargo
exit 0 · graph REGENERATED and CURRENT (1,033,135 bytes — the regen
this record's own commit carries). Gate runtime: the e2e reporter's
3.2m line in the battery output. Metrics: rework 0; executor 211,873
tokens (73 uses, 27 min). Cold start: NONE. Drift incidents: NONE.
Lanes after: T-025-s6 only (verifying, verifier seat live).
