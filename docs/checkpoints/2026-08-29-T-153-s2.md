# Checkpoint: T-153-s2 (2026-08-29, integrator nputer-4e, direct pipeline)

The last CI blocker lands, and the work corrected its own card: the
200,000-byte string the card blamed on argv travels by the child
ENVIRONMENT, and Linux's `MAX_ARG_STRLEN` bounds envp exactly as it
bounds argv. From this merge every pair the runner hands `execve` goes
through one function — `set_child_env` — and an oversized pair is
dropped WHOLE, named on stderr without its value, with the turn
standing. The push-hold lifts AT this checkpoint.

## Merge

Parents `78aabe5` (main, the T-155/T-156 dispatch commits atop the
amnesty checkpoint) and lane tip `d0d866e`, `--no-ff` -> `d969c8c`.
The merge's diff: **6 paths** — adapter.rs (the bound and
`child_env_pair_fits`), runner.rs (`set_child_env`, every pair routed
through it), the integration tests, the card, and two suggestion cards
(`T-153-s3`, `T-153-s4`). Verdict: **APPROVED WITH ONE ASSIGNED
CORRECTION**, executed here (below). The executor REFUSED one brief
instruction — a fixture cannot cross the bound and keep
`registry.model == None` — and the verifier reproduced the
impossibility (its M10) rather than taking the argument: the refusal
was correct, and arm (a) of the card was false as written.

## The verdict's assigned correction, performed here

The verifier's mutants M5/M6 (allowlist loop and PATH chain each
bypassing `set_child_env`) left the whole suite green — the bound's
PRODUCTION arms were killed by nothing. Performed at this seat:
`an_oversized_path_or_allowlist_pair_meets_the_bound_on_the_production_channels`
in tests/agent_runner.rs — the PATH arm through `path_override` (a
real product field, first branch of the chain) present WHOLE at the
exact widest fit and absent one byte over with the turn still
standing; the allowlist arm through a planted `HTTPS_PROXY`, riding
small and dropped past the bound. `Options` gained a `path_override`
passthrough (default preserves the standard test PATH other bodies
assert byte-for-byte). **Acceptance was mechanical and is stamped**:
drill at a detached worktree (`d969drill`, own `CARGO_TARGET_DIR`,
planted body) — baseline 0, M5 exit 101, M6 exit 101, runner.rs
restoration sha256-MATCH `d969c8c` (13ed6cc4…), worktree removed, no
graph touched inside the drill (T-153-s3's rule, followed on its first
day).

## Gates

- GRAPH REGEN: OWED (3 .rs paths) and performed here — 1,021,562
  bytes, 189 files, 2,157 symbols, 2,111 edges; 98.2%, 18,438 left.
- BOOT GATE: OWED and run — first attempt ABORTED itself because the
  human's app holds port 1420 (the gate's own guard); re-run on
  scratch port 16418 after an lsof zero-rows read: exit 0.
- DOCS GATE: `lint:docs` exit 0. capabilities:check CURRENT (20,046
  bytes — the new bodies are cargo tests, not e2e specs).

GRAPH, asked LAST: after this record's final write — CURRENT,
1,021,562 bytes, exit 0.

## Suites (counts and exits, unpiped)

cargo exit 0 — lib **200** in 6.24s (under the cliff), agent_runner
**82 passed + 1 ignored** (the assigned-correction body among them),
**188** others · parser **314/314** exit 0 (dist rebuilt first) · app
**1013/1013** exit 0 · e2e **258/258** exit 0 (port 16318, lsof zero
rows first) · boot exit 0.

## CI

The lane ran on draft PR #2: the ubuntu cargo step went green THREE
times (runs 33255912812 / 33256467475 / 33257012982) — the first runs
in this repository's history to clear that step — each then stopping
at `graph currency`, exit 1 by design (the regen is this seat's, and
it happened above). Steps 19–26 have NEVER executed on Linux: the
push this checkpoint authorizes is a FIRST RUN, watched as one.

## Environment

Lanes after this checkpoint: T-155 and T-156 (dispatched this sitting,
executors running). T-153-s2's worktree REMOVED, branch kept; the
executor's leftover drill worktree `t153s2-drill` REMOVED (the
verifier flagged the litter; it held only drill logs at `b5e3e4e`).
Port 1420 untouched (read once, by lsof, and the boot gate's own
abort proved the discipline); `z` unmoved.

## What the seats disagreed about, routed

- The verifier followed `method/roles/verifier.md` (which EXCLUDES
  docs/ROADMAP.md from its read set, with a reason) over my brief
  (which instructed reading it) — the right call, and the conflict is
  T-159 review-reconciliation material: a dispatcher's brief must not
  contradict the role file it cites.
- `T-153-s4` (two spawn sites inherit the parent env whole — doors
  the bound never reaches, theoretical today) and `T-153-s3` (the
  drill target dir is INSIDE the graph walk; found independently by
  two seats in one day) arrive on the board as suggested.

## Metrics (ADR-020)

Rework cycles: **0** — the assigned correction was integration-seat
work, not a re-dispatch. Tokens, stamped from the meters: executor
**292,727** (205 tool uses, 48 min) + verifier **237,223** (96 uses,
28 min) ≈ **530k**. The arc series: 484k / 573k / 795k / 477k (140
cards) / 530k. Note the verifier's two-phase blindness (attack set
from the card BEFORE the notes) found the one defect the executor's
own "least confident" line pointed at — the discipline earned its
keep on its first outing.
