# Sealed verifier inputs

This directory is reserved for the post-verdict input records described by T-205-s6. That card defines which inputs are retained, who records them and when, and the append-only record policy. Until that work lands, this directory contains this bootstrap note only.
