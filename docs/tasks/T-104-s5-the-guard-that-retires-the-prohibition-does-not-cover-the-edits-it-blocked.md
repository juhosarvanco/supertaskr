---
id: T-104-s5
title: The guard whose arrival retires T-104's RANGE RULE prohibition does not actually cover either edit the prohibition blocked — and the five CONVENTIONS edits queued at that seat sit in exactly two bullets
status: parked
suggested_by: integrator claude-opus-5 @T-104
---

Absorbs: T-092-s7 (Amnesty triage 2026-08-29 (triage seat)) — a queue member for the RANGE RULE bullet, and the instance behind it needs nothing in the tree — the answer was right because main never moved during that lane, so the banned pair and the prescribed one collapsed onto each other. What survives is the arm: the two banned forms are currently disclaimed in DIFFERENT VOICES (merge-base..tip by name, <main>..HEAD in a later paragraph), so a session quoting one ban can land in the other, which is exactly what happened here with a compliance label attached.

Absorbs: T-091-s3 (Amnesty triage 2026-08-29 (triage seat)) — queue member, needle re-checked live at this base: docs/CONVENTIONS.md still states the flip figures "Derived at ddcc8bb" with no sentence naming the trigger they were matched against, and GRAPH REGEN's trigger has since gained *.rs. Its own instruction is preserved — name the trigger, do NOT replace the figures, because the older larger sample is the better evidence for the mechanism the paragraph argues.

Absorbs: T-104-s3 (Amnesty triage 2026-08-29 (triage seat)) — the first statement of this queue, and this card exists to correct its closing argument: s3 concluded the two RANGE RULE edits were safe once T-091's reader landed, and the reader is a regression guard on the FIGURES that would report 25 of 25 whether the new prose is right or wrong. Its general finding — a fence names paths and this project writes prohibitions at paragraph grain — is kept here as the reason the queue formed at all.

**T-104's fence is released by its merge, so FIVE earned
`docs/CONVENTIONS.md` edits are now takeable and none of them is taken in
that checkpoint.** This finding records why, names a target for each, and
corrects the argument that everyone — the lane, `T-104-s3`, and this
integrator's own dispatch brief — used to conclude they were now safe.

**THE COUNT IS DERIVED HERE AND IT IS TWO MORE THAN THE BRIEF DESCRIBED.**
The brief named two routed edits plus one new one. `T-079-s3` items 2–3
and `T-130-s1` are also sitting at this seat, and **all five land in
exactly TWO bullets** — two in the RANGE RULE bullet and three in the
POISON DRILL bullet. `T-130-s1` says so itself, asking to be taken *"in
one edit with them rather than as a fourth pass over the same bullet."*
**That is the routing fact worth more than any individual sentence: five
edits, five seats of origin, two paragraphs.**

## THE CORRECTION, WHICH IS THE POINT OF THIS FILE

`T-104-s3` closes by saying the next CONVENTIONS card *"should re-run
`tools/e2e/tests/range-rule.spec.ts` afterwards to prove the roughly
thirty parsed figures still hold — it is 25 of 25 green today."* The
dispatch brief for this integration went further: *"the reason for that
prohibition is now spent — it existed because T-091's reader was
unmerged and the bullet was unguarded; T-091 has since landed, so the
reader will red if anyone breaks it."*

**THAT IS TRUE OF THE FIGURES AND FALSE OF THE TWO EDITS.** Measured at
the merge `f309405`, where the reader ran as part of the owed
`tools/e2e` suite: **171 passed, exit 0**, `range-rule.spec.ts` green.
And **neither routed edit changes a figure**:

- **`T-091-s3`'s trigger-beside-the-ref clause** ADDS a sentence naming
  GRAPH REGEN's trigger next to flip figures it leaves untouched.
- **The `bdada11` sharpening** rewrites a COMMAND RECIPE — prose about
  `merge-tree` exiting 1 while still printing a tree oid on line 1.

So `range-rule.spec.ts` would report **25 of 25 whether the new prose is
right or wrong.** The reader is a REGRESSION GUARD ON THE FIGURES; it is
not a review of new normative text, and it cannot become one, because
the thing being added is exactly the thing it does not parse. **A guard
that cannot fail on the change you are making is not protection for that
change** — which is T-104's own ruling THREE (*a criterion can specify a
check that cannot fail*) arriving from outside the card, at the card's
own merge, about the card's own prohibition.

**The consequence is small and specific**: the prohibition is genuinely
spent and both edits should land, but they want the ordinary ceremony —
a card, an executor and a verifier — rather than an integrator folding
them into a checkpoint on the strength of a green reader. The
integrator's rule (*repair what the merge INTRODUCES, file what the merge
merely REVEALS*) reaches the same answer by a shorter road: this merge
neither writes these sentences nor falsifies them.

**There is a second, sharper reason not to fold the two RANGE RULE ones**,
independent of ceremony: **the RANGE RULE bullet is the paragraph that
governs the very range the checkpoint is computing.** A checkpoint that
edits it in the same commit hands its own reader a bullet that moved
under them — `T-128`'s class, in the "correct edit made on the wrong
side" shape its fourth instance already names.

## THE FIVE EDITS AND THEIR NAMED TARGETS

### THE RANGE RULE BULLET → `T-093`, `status: planned`, `touches: [docs/CONVENTIONS.md]`

`T-091-s3` and the `bdada11` sharpening go to **`T-093` — "A search that
finds nothing is not a refutation, a charset is not the gate, and a
backtick in a label runs the command: the hand's measurement hazards,
written where the hand reads."** This is not a convenience routing:

- The `bdada11` sharpening IS a hand's measurement hazard, verbatim — a
  command substitution swallows `merge-tree`'s exit and hands you *"an
  EMPTY forecast wearing the costume of a clean gate"*. This integration
  obeyed it (exit read from `$?` **before** the substitution: **0**, tree
  `8954d56`) and it is unwritten in the sharpened form.
- `T-091-s3` is the same shape from the other side: a figure that is
  RIGHT at its stated ref and READS wrong because the paragraph carries
  no trigger. **The reader printed exactly this disclosure at the merge
  commit** — *"GRAPH REGEN's published flip figures are stated at
  `ddcc8bb` and ARE RIGHT THERE, and its trigger has since gained `.rs`:
  5 of 5 at that ref, 1 of 1 under the trigger on disk"* — so the residue
  is observed at `f309405`, not theoretical.

T-093 holds `docs/CONVENTIONS.md`, is `blocked_by: []`, is size S, and
**carries no RANGE RULE prohibition** — T-104's existed only because
T-091 was in flight, and T-091 merged at `ca5fb96`.

### THE POISON DRILL BULLET → `T-092`, `status: planned`, `touches: [docs/CONVENTIONS.md, app-agent]`

**THREE EDITS, FROM THREE SEATS, IN ONE PARAGRAPH — AND THEY SHARE A
BLIND SPOT.** `T-079-s3` items 2–3 and **`T-130-s1`** (*"what restoring a
fixture MEANS belongs in the POISON DRILL bullet — and the ctime caveat
beside it is weaker than it reads"*) have been queued at this seat since
T-130's dispatch, and `T-130-s1` explicitly asks to be taken **in one
edit** with `T-079-s3` rather than as a fourth pass over the same bullet.
Their sentence is that **a content-exact restore is not a restore** — a
body that plants into a tracked file restores its BYTES *and* its CLOCK,
and a clock restored through a `Date` is lossy below the millisecond.
The third is new tonight:

**T-129's verifier retracted a determinism defect after discovering it
was its own**, and the generalisation belongs in the same bullet: *a
drill that mutates a constant poisons every later BINARY-LEVEL
measurement in that target directory unless you rebuild.*
It read `index --check` reporting a fresh index 156 bytes larger than
`index --root` had written, and began writing it up against ADR-014;
`cargo test` had rebuilt the package binaries, so its `MAX_DEPTH = 32`
drill left a mutated `nputer-index` behind.

**THE BULLET CANNOT SEE ANY OF THE THREE, AND THE REASON IS THE SAME
SENTENCE EACH TIME — ITS OWN RESTORATION PROOF.** It requires restoration
to be PROVEN — *"`git show HEAD:<path> | shasum -a 256` against the
working file, or an empty `git diff -- <path>`"* — and **both are checks
over SOURCE-FILE CONTENT**. A restored source with a stale binary beside
it passes every one of them; so does a restored source whose mtime moved.
**The bullet rules how a restoration is PROVED and never says what
restoring MEANS**, and all three edits are that one gap seen from three
angles. **Take them together or the bullet gets patched three times and
still does not say it.** The bullet
already carries the two neighbouring hazards (`env!("CARGO_MANIFEST_DIR")`
baked in at compile time, and *"a mutant can look DEAD against a stale
binary"*), and **its remedy — arm (c), a scratch worktree with its own
`CARGO_TARGET_DIR` inside it — does not reach this case at all**: arm (c)
protects the PARENT's cache, and T-129's poisoned measurement was taken
inside the drill's own worktree with its own target directory, after the
restore.

**`T-092` is the right home by subject, not just by fence**: it is *"the
poison taxonomy pass — a containment pin with no uniqueness floor, three
unnumbered shapes, and four places the drill's own procedure cannot
fail."* This is a fifth such place, and it is the first one where the
drill's PROOF OF RESTORATION is what fails.

**Route rather than fold, for one hard reason beyond ceremony: `T-129`
AND `T-130` ARE BOTH UNMERGED.** T-129's verdict is at `73651cb` on
`task/T-129-nesting-abort` and T-130 is still building on
`task/T-130-mtime-restore`; writing an unmerged lane's finding into main
ahead of its own integrator would be taking a second lane's content
through a first lane's checkpoint. **Neither fence reaches
`docs/CONVENTIONS.md`** — `[crate-index]` and `[tools/e2e]` — so BOTH
integrators will meet this same routing question. That is **`T-104-s3`
firing a second and third time, from two different lanes, before that
finding has been triaged**, which is the strongest evidence available
that s3 is a real class and not one card's accident.

## FENCE

`[docs/CONVENTIONS.md]` for the edits themselves; this file needs none.
The file is **free** after T-104's merge. All three edits sit outside any
other live lane's fence (`app-shell` T-126, `crate-index` T-129,
`tools/e2e` T-130).

Amnesty triage 2026-08-29 (triage seat): PARKED — the queue is real and its routing lesson is durable — five edits, five seats of origin, two paragraphs — but the card's own two named homes have since MERGED (T-093 at 91413c8, T-092 at 162b5ed) and took part of it, so the list on this card is a transcribed census and must not be read as one. Re-derived at this base: T-091-s3's trigger clause is still absent from the RANGE RULE bullet, and the POISON DRILL bullet has gained "A RESTORE CANNOT TELL ITSELF FROM A REVERT" and the scratch-snapshot cmp proof without gaining what restoring MEANS. RESURFACES: the next docs/CONVENTIONS.md dispatch, which SHALL re-derive the queue rather than quote this card — that is this card's own lesson turned on itself. The surviving members at this base are T-091-s3 (absorbed here) and the drill-restoration class owned by T-092-s4.
