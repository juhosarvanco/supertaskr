---
id: T-021
title: Shell IPC hardening — pin the zero-surface proof, throttle the picker
feature: F-02
milestone: 4
priority: 12
size: M
status: done
blocked_by: []
touches: [app-shell]
builder: claude-fable-5
verifier: claude-fable-5
built_by: "claude-fable-5 @fresh"
verified_by: "claude-fable-5 @fresh"
review: same-model
---

Absorbs: T-007-s2, T-007-s3. Triage 2026-08-15. Must land before
F-04 (dispatch), when the webview grant set first moves.

## Acceptance criteria
- THE cargo suite SHALL gain an ACL-surface regression test built on
  the T-007 verifier's demonstrated machinery (resolved authority
  from shipped artifacts + real InvokeRequests): dialog/fs/opener
  commands denied, app commands allowed, remote origins denied —
  the zero-webview-surface proof pinned, not re-derived per task.
- THE pick_project_folder command SHALL be single-flight (AtomicBool;
  concurrent invocations get a typed busy result) and the
  validate→arm mutex window SHALL be narrowed per the T-007
  verifier's residual notes.
- IF the ACL surface changes in any future diff THEN this test SHALL
  fail with a diff of the grant set (the alarm F-04 needs).

## Implementation notes

Executor claude-fable-5 @fresh, 2026-08-16, branch `t021-ipc-hardening`.
Branch point: main@7e28f30 (the dispatch commit) — the T-019 merge
landed on main while this worktree was being created; the branch was
deliberately reset to 7e28f30 so the dispatch baselines hold literally
(verified: `git merge-base HEAD main` = 7e28f30; measured baselines
below match the dispatch numbers exactly). Merge-base reconciliation is
the integrator's, per dispatch.

### What was built

**Criterion 1+3 — the ACL surface pinned, with a diff-producing alarm**
(`app/src-tauri/src/acl_pin.rs`, new, `#[cfg(test)] mod acl_pin` in
lib.rs; absorbs T-007-s2). Five tests on the T-007 verifier's exact
machinery:

- `t021_webview_grant_set_is_pinned_and_diffs_on_movement` — parses the
  SHIPPED artifacts `gen/schemas/acl-manifests.json` +
  `gen/schemas/capabilities.json` (both regenerated by tauri-build on
  every `cargo build`/`cargo test`, so the pin always reads the
  compiled truth of the CURRENT tree, never a stale fixture), resolves
  them through tauri's own `Resolved::resolve` (`tauri::utils::acl`,
  the routine tauri-build itself runs), and compares the rendered grant
  set — one sorted line per grant: `command @context windows=[...]
  webviews=[...]` — against `EXPECTED_GRANTS`: the 92 grants
  `core:default` resolves to on this tree, and nothing else. On
  mismatch it panics with `+ granted now, not pinned` / `- pinned, no
  longer granted` bullet blocks plus the full current set formatted for
  re-pinning (criterion 3's diff). Also pins empty `denied_commands`,
  empty scopes, and `has_app_acl == false` (the app-command gating
  model itself).
- `t021_capability_source_is_exactly_core_default_local_main` — the
  COMMITTED source: capabilities/ contains exactly `default.json`;
  identifier/windows/permissions exactly `default`/`["main"]`/
  `["core:default"]`; no `remote`, no `webviews`, local true. The
  GENERATED artifact agrees field-for-field (a stale or bypassed
  tauri-build shows up here). Registered namespaces pinned: `dialog`
  present (denial below is an ACL decision, not absence), `fs` and
  `opener` ABSENT.
- `t021_real_invokes_dialog_fs_opener_denied_app_allowed_remote_denied`
  — the resolved authority INSTALLED into a MockRuntime app
  (`mock_context` + `Context::runtime_authority_mut`, via the
  `tauri::runtime_authority!` macro) with the real
  `tauri_plugin_dialog::init()` registered and the real `docs_snapshot`
  in the invoke handler over a managed `WatchState`; probed with real
  `InvokeRequest`s (real `on_message` path, real invoke key, real
  origin classification). From `tauri://localhost`:
  `plugin:dialog|open`/`save`/`message` denied — debug detail names the
  ungranted dialog permissions (denial-not-absence); `ask`/`confirm`
  denied as UNDECLARED names ("Command not found" — the dialog JS
  ask/confirm APIs compile to the `message` command; the manifest shows
  allow-ask/allow-confirm/allow-message all allowing `message`; a
  sharper truth than T-007's five-command phrasing, same wall); eight
  `plugin:fs|*` and three `plugin:opener|*` commands denied "Plugin not
  found"; `docs_snapshot` EXECUTES and answers `{"kind":"noProject"}`
  (positive control through the real handler + real state);
  `plugin:event|listen` passes the ACL gate. From
  `https://evil.example.com`: `docs_snapshot`, `pick_project_folder`,
  `index_repo`, `plugin:event|listen`, `plugin:dialog|open` ALL denied
  (T-007's bonus finding — local-only capability — now pinned).
  Headless by construction: MockRuntime creates no OS window and every
  dialog invoke dies at the ACL before plugin code could run;
  `pick_project_folder` is never invoked from a local origin anywhere.
- `t021_authority_resolves_no_dialog_fs_opener_command_but_core_default_yes`
  — authority-level cross-check: `resolve_access` returns None for
  EVERY command the dialog manifest itself declares (enumerated from
  the manifest, so new upstream dialog commands are auto-probed), for
  fs/opener names, for non-`main` windows, and for remote origins even
  on granted commands; `plugin:event|listen` resolves for main/local.
- `t021_grant_set_rendering_is_deterministic` — resolve-twice equality
  (BTreeMap ordering end to end; no hash/set-iteration luck in the pin).

**Criterion 2a — single-flight picker** (absorbs T-007-s3).
`WatchState` gains `picking: Arc<AtomicBool>`;
`WatchState::begin_pick()` compare_exchanges it and returns
`Option<PickInFlight>` — an RAII guard whose Drop releases the latch on
every path, panic included. The `pick_project_folder` command (lib.rs)
claims the guard BEFORE opening the native dialog; refusal is the new
TYPED `PickOutcome::Busy` (wire shape `{"kind":"busy"}`, pinned by
test), so a double-click race or a compromised webview can never stack
native dialogs — the T-007 verifier's observation (1).
`apply_picked_folder` now CONSUMES a `PickInFlight` (signature change),
making "the validate→arm→commit pipeline runs under the latch"
structural — no unguarded caller can exist. The guard travels into the
spawn_blocking task, so it is held from pre-dialog to post-commit.

**Criterion 2b — validate→arm mutex window narrowed** (the T-007
verifier's residual note (2), implemented as named). Pre-T-021,
`apply_picked_folder` held the project mutex across canonicalize →
validate → re-arm rendezvous (up to `REARM_TIMEOUT` = 10s) → commit, so
a concurrent `docs_snapshot` blocked behind a slow re-arm. Now the
mutex is taken for the COMMIT alone (`*state.project.lock() =
Some(..)`, one statement); the rendezvous runs lockless. Soundness
argument, recorded: (a) picks cannot interleave with picks — the flight
guard serializes the whole pipeline (that is why 2a and 2b are one
criterion: the narrowing is safe BECAUSE of the guard); (b) the
validate→arm TOCTOU window was never protected by the mutex — the
watcher thread's own `has_plain_docs_dir` gate re-checks at arm time
(T-007's defense in depth, verifier-attacked then); (c) seq ordering
across the switch is unchanged: ack → commit → seq, exactly the old
sequence, so the pick snapshot still outranks every pre-pick emit and
the reset-baseline invariant ("snapshot never older than baseline") is
untouched.
Also implemented from the residual notes, declared: observation (4) —
the rendezvous `recv_timeout` now distinguishes `Timeout` ("watcher
re-arm timed out") from `Disconnected` ("watcher thread dropped the
re-arm ack"), so a dead watcher thread reports instantly and honestly
instead of a fabricated 10s "timeout". Observation (3) (unsanitized
picked-path println) deliberately NOT taken: the verifier classed it
with T-001's project-folder line — the local user's own choice, not
repo content — and named no action.

**Frontend seam** (`app/src/lib/watcher-store.ts`, the only app/src
touch): `PickOutcomePayload` union gains `{ kind: "busy" }` and
`pickProjectFolder`'s switch gets an explicit no-change `busy` case.
The store's own `picking` gate makes webview-side Busy near-unreachable
(it exists for OTHER webview contexts / compromised callers — the Rust
latch is the real gate). No new vitest test: this suite has no IPC
mocking machinery (the T-007 verifier's component probe was a
deliberate throwaway), the union is tsc-checked by `npm run build`, and
the wire contract is pinned Rust-side.

### Tests added (cargo, app crate 41 → 49; workspace 121 → 129 + 2 ign.)
docs_watch (3, all deterministic — hold/assert/release, no sleeps; the
only timeouts bound FAILURE modes, never the pass path):
- `second_pick_claim_is_refused_typed_until_the_first_releases` —
  exclusion + `{"kind":"busy"}` wire pin + release-on-drop.
- `a_parked_rearm_blocks_neither_docs_snapshot_nor_leaks_the_latch` —
  a stand-in watcher thread receives the Rearm, signals, then PARKS
  until released (deterministic slow re-arm). While parked:
  `begin_pick` refused; `project_status` (the docs_snapshot body)
  answers `NoProject` from a helper thread — THE narrowed-mutex proof —
  and proves commit-not-yet-happened; on release: `Picked`, project_dir
  committed once, latch free again.
- `a_dropped_rearm_ack_reports_disconnect_mutates_nothing_frees_latch`
  — ack sender dropped mid-re-arm → instant typed
  `Error{"...dropped the re-arm ack"}`, project_dir unchanged, latch
  released.
acl_pin (5, above). Existing tests: the 12 docs_watch pick call sites
moved to an `apply_pick` helper (begin_pick + apply — the command layer
minus the dialog); index_cmd's one call passes a fresh guard inline;
zero behavioral edits to any pre-existing test.

### Criteria → evidence map
- **C1 (pinned ACL regression test on T-007 machinery)**: acl_pin.rs —
  resolved authority from shipped artifacts (`shipped_manifests` /
  `shipped_capabilities` / `resolve_shipped`) + real InvokeRequests
  (`t021_real_invokes_...`): dialog/fs/opener denied, `docs_snapshot`
  allowed, remote origins denied. Runs in bare `cargo test` forever.
- **C2 (single-flight + narrowed window)**: `begin_pick`/`PickInFlight`
  + `PickOutcome::Busy` (docs_watch.rs), command guard (lib.rs),
  commit-only lock (`apply_picked_folder` tail); the three docs_watch
  tests above.
- **C3 (fails with a grant-set diff)**: `EXPECTED_GRANTS` + the panic
  block in `t021_webview_grant_set_...`; fired end-to-end, below.

### The alarm demo (criterion 3, executed then reverted)
1. REAL-PIPELINE MOVEMENT: added `"dialog:allow-open"` to the committed
   `capabilities/default.json` and ran `cargo test` — tauri-build
   regenerated gen/schemas from the mutated source, and FOUR of the
   five acl_pin tests failed independently: the pin panicked with
   `+ granted now, not pinned: plugin:dialog|open @local windows=[main]
   webviews=[]` (nothing under `-`), the capability-source pin caught
   `permissions != ["core:default"]`, the runtime probe caught
   `plugin:dialog|open` suddenly ALLOWED, and the authority cross-check
   caught it resolving. Reverted (`git checkout capabilities/`), suite
   green again. One movement, four independent alarms, the diff names
   it.
2. BOTH DIFF DIRECTIONS: the first-ever run against a deliberately
   wrong placeholder pin printed `+` lines for all real grants absent
   from the pin AND `-` lines for pinned-but-not-granted inventions
   (e.g. a guessed `plugin:app|app_hide`), plus the full 92-line
   re-pin list — the removal direction exercised, not assumed.
3. MUTATION TEST on criterion 2b (does the regression test detect the
   regression?): temporarily restored the pre-T-021 broad lock (mutex
   held across the rendezvous) — `a_parked_rearm_blocks_neither_...`
   failed exactly as designed: `docs_snapshot must answer while a
   re-arm is parked (pre-T-021 it blocked here): Timeout`. Reverted,
   green.

### Verification (macOS/Darwin 25.6, rustc 1.95.0, node 22.22.0)
From the worktree, ADR-011 order:
- lib/parser: ZERO-BYTE diff vs 7e28f30 (`git diff 7e28f30 --
  lib/parser | wc -c` = 0); `npm ci` + `npm run build` (app
  prerequisite); `npx vitest run` **132/132**; `npx tsc --noEmit`
  clean.
- app/src-tauri: `cargo test` **129 passed + 2 ignored, 0 failed** —
  THREE consecutive full-workspace runs, identical counts, no flakes
  (baseline 121+2; +3 docs_watch, +5 acl_pin); `cargo build` exit 0.
- app: `npm run build` exit 0 (tsc + vite); `npm test` **392/392**
  (baseline held; no new vitest tests — reason above).
- Cargo.lock: ZERO diff. tauri.conf.json, capabilities/: zero diff.
  No servers, no ports, no port-1420 contact, no screen control, no
  real model calls, headless throughout.

### Dependency justification
NO new crates, npm packages, or version constraints. The one manifest
change: `[dev-dependencies] tauri = { version = "2", features =
["test"] }` — the SAME version spec as the existing main dependency
(cargo unifies them; exact-pinning only here would silently exact-pin
the main dep too, a semantics change this task must not smuggle),
enabling tauri's `test` feature for test builds only. Upstream
`test = []` is an EMPTY feature: zero new transitive crates, and the
lockfile proves it (zero-byte diff). `tauri::utils` re-exports
tauri-utils, so the ACL types cost no dependency line either.

### Flags for the verifier
- The pin reads `gen/schemas/*` (gitignored, tauri-build-regenerated).
  `cargo test` always runs build.rs first, so the artifacts are always
  the CURRENT tree's compiled truth — precisely what makes the
  capability-mutation demo end-to-end. If tauri-build somehow did not
  run, the test fails loudly at the unreadable-file panic, never
  silently passes.
- `EXPECTED_GRANTS` is macOS-derived; the manifests are target-filtered
  at build time and resolution uses `Target::current()`, so the Linux
  lane may print a diff → decision filed as T-021-s1 (per-platform
  pins vs normalized projection). The other four acl_pin tests are
  platform-independent.
- Denial-message DETAIL asserts ("dialog:", "Plugin not found",
  "Command not found") are `#[cfg(debug_assertions)]`-gated: release
  builds reject with a terse "not allowed by ACL" string. The core
  "not allowed" assert is unconditional. (`cargo test --release` kept
  in mind by construction; not part of the gate, not run.)
- `pick_project_folder`/`index_repo` could not register on MockRuntime
  (Wry-monomorphic `tauri::AppHandle` — `CommandArg<MockRuntime>`
  unsatisfied). `docs_snapshot` (State-based, runtime-generic) is the
  executing local positive control — the T-007 verifier's own choice;
  the ACL layer treats all app commands identically by construction
  (name-based decision BEFORE handler lookup + `has_app_acl == false`,
  both pinned). Remote-origin denials for all three are probed and
  provably precede dispatch. Filed as T-021-s2.
- PRE-EXISTING race, unchanged in kind by the narrowing, named so
  nobody re-derives it: `project_status` clones the project dir under
  the lock, then takes its seq and collects OUTSIDE it (T-007 shape).
  A `docs_snapshot` that read the OLD dir before a pick's commit can
  take a HIGHER seq than the pick's snapshot and deliver an
  old-project `Open` late; the frontend would reset to the old project
  until the next emit heals it. Under the OLD broad lock the identical
  interleaving existed (status clones-then-releases BEFORE the pick's
  lock acquisition, seq taken after); the narrowing only shrinks how
  long a status call can be DELAYED, never what it can observe out of
  order. Unreachable from the shipped store (`docs_snapshot` is called
  once, at init, before any pick can exist); a compromised webview can
  only confuse its own view, self-healing on the next docs-changed.
  Recorded, not fixed — fixing it means stamping status reads with
  commit-ordering they currently do not need.
- The store's webview-side `picking` guard stays: redundant with the
  Rust latch for serialization, still load-bearing for the disabled
  button state, and defense in depth.

### Suggestions filed
- `T-021-s1-acl-pin-platform-scope.md` — per-platform EXPECTED_GRANTS
  decision when the Linux lane lands (feeds T-020).
- `T-021-s2-generic-runtime-app-commands.md` — genericize
  AppHandle-taking commands over `R: Runtime` so the pin can register
  the full real handler set on MockRuntime.

## Verdicts

2026-08-16 — claude-fable-5 @fresh (verifier, same-model as builder):
APPROVED.

Suites re-derived (macOS/Darwin 25.6, ADR-011 order): src-tauri bare
`cargo test` **129 passed + 2 ignored, three consecutive identical
runs**; `cargo build` exit 0; app `npm run build` exit 0 + `npm test`
**392/392**; lib/parser ZERO-byte diff vs 7e28f30 (the untouched claim
is literal) + `npx vitest run` **132/132**. The three new docs_watch
concurrency tests ran 6 further consecutive times, all green in <10ms —
the hold/assert/release rendezvous is deterministic as claimed; every
timeout in them bounds a failure mode only, never the pass path.

Criteria 1+3 (the pin + the alarm), attacked:
- Alarm demo REPRODUCED end-to-end: `dialog:allow-open` added to the
  committed capabilities/default.json, bare `cargo test` — tauri-build
  regenerated gen/schemas from the mutated source and FOUR independent
  tests failed: the pin panicked with exactly
  `+ granted now, not pinned: plugin:dialog|open @local windows=[main]
  webviews=[]` (removal side "(none)"), the source pin caught the
  permissions drift, the authority cross-check caught it resolving, and
  the runtime probe caught `plugin:dialog|open` suddenly PASSING the
  ACL — failing on missing args INSIDE the real plugin, which
  simultaneously proves the probe drives the genuine invoke pipeline
  and empties the stale-copy worry (the pin reads the current tree's
  compiled truth, demonstrably). Reverted, green.
- The REMOVAL direction exercised by me, not taken from the notes:
  shrinking permissions to `core:event:default` printed the 88 `-`
  lines and the 4-grant re-pin list. Both diff directions live.
- EVASION attempts, each caught four-ways: a SECOND capability file
  (capabilities/extra.json granting dialog:allow-open) → file-listing
  pin + grant diff + runtime probe + authority check all fired; a
  `remote` key on default.json → source pin plus per-grant
  `@remote:https://evil.example.com` context lines in the diff (the
  rendering encodes execution context, so remote movement is
  enumerated grant-by-grant). Reasoned the rest: inline tauri.conf.json
  capabilities land in the same compiled artifact the pin reads;
  window/webview scoping is pinned per-grant AND at source;
  denied_commands / scopes / has_app_acl pinned; a NEW plugin
  registration without grants moves nothing reachable (the registered-
  but-ungranted dialog plugin is the standing proof of that wall) and
  with grants is caught; fs/opener registration specifically pinned
  ABSENT by namespace.
- The Wry-monomorphism construction argument RULED SOUND — and upgraded
  from argument to evidence: my own probe invoked the UNREGISTERED
  `pick_project_folder`, `index_repo`, and a made-up name from the
  LOCAL origin — all three died at handler lookup ("Command X not
  found"), never at the ACL; the shipped remote-origin probes deny the
  SAME names before handler lookup. Together: the ACL decides app
  commands by name-class + origin, identically, before any handler
  exists. `docs_snapshot` as the sole EXECUTING local control plus the
  pinned `has_app_acl == false` covers criterion 1's "app commands
  allowed" honestly; T-021-s2 remains the right upgrade path.

Criterion 2 (single-flight + narrowed window), attacked:
- RAII paths enumerated from the code: Busy (never claims), cancelled,
  into_path error, ctl-send error, rearm Err, Timeout, Disconnected,
  commit — all release via Drop on return; the command future dropped
  mid-dialog releases (the guard is a plain local until it moves into
  spawn_blocking); the PANIC path — the one no shipped test drives —
  probed by me: catch_unwind over a panicking holder, latch free
  after. No leak path found; no panic="abort" profile anywhere → s3
  filed to make the panic path a permanent test.
- `{"kind":"busy"}` wire shape pinned Rust-side; the +7 in
  watcher-store.ts are exactly the union member + an explicit
  no-change case; ZERO new invoke calls in app/src; 392/392 holds.
- THE TOCTOU RULING: the narrowing does NOT reopen validate→arm.
  Re-derived from the code, not the notes: (a) the project mutex never
  excluded filesystem mutation — external writers take no process
  lock, and the watcher thread provably CANNOT take this one (it never
  receives WatchState; only the seq Arc, the ctl receiver, and the
  sink); (b) the T-003 rule family runs at three layers, all still in
  place — pick-time `has_plain_docs_dir`, the thread's OWN
  `has_plain_docs_dir` gate at arm time inside `rearm()`, and
  read-time refusal of symlinked docs/ + per-file canonical
  containment in `collect_docs_tree`; a symlink swap or folder
  replacement between validate and arm is refused by the thread's gate
  (typed Err, nothing committed — the T-007 verifier's own attack,
  machinery unchanged), and one between ack and snapshot collapses to
  an empty/contained read, never a leak; (c) T-018's arm-time identity
  re-derivation (`dir_identity` in `ensure_docs_watch`) is untouched;
  (d) pick-vs-pick serialization strictly STRENGTHENED: mutex-at-apply
  became a consumed guard claimed before the dialog opens, private
  constructor — no unguarded pipeline can compile; (e) commit ordering
  ack→commit→seq is identical to pre-T-021 (diffed against 7e28f30).
  MUTATION TEST reproduced: restoring the broad lock fails
  `a_parked_rearm_blocks_neither...` with exactly "docs_snapshot must
  answer while a re-arm is parked (pre-T-021 it blocked here):
  Timeout" — the regression test detects the regression. Reverted.
- The pre-existing status/pick seq race: confirmed pre-existing IN
  KIND by walking the OLD lock ordering (a status that cloned the old
  dir and released before the pick's lock acquisition could always
  take a later seq than the pick's); confirmed unreachable from the
  shipped store (`docs_snapshot` invoked exactly once, at init, behind
  the `started` latch — read, not assumed). Narrowing shrinks reader
  DELAY only. Stays a note, correctly.

Residual-notes fidelity, judged against the actual T-007 verdict text:
observation (1) → this task's single-flight guard (absorbed s3);
(2) — "the rendezvous holds the project mutex up to 10s" — implemented
as named (commit-only window; the parked-rearm test is its proof);
(4) — "a lost re-arm ack reports 'timed out' even when instant" —
implemented (Disconnected arm, typed message, tested); (3) — the
unsanitized picked-path println — the T-007 verifier classed it with
T-001's project-folder line and named NO action; skipping it is
faithful, not evasive. Fidelity: exact.

Security sweep: capabilities/, tauri.conf.json, Cargo.lock all
ZERO-byte diff vs 7e28f30 — the task's whole point holds at the byte
level. acl_pin.rs (628 lines) reaches no production build: single
`#[cfg(test)] mod` reference, `cargo build` clean, and the feature
graph proves the dev-dep's `test` feature stays out of the normal
resolve (`cargo tree -e normal`: tauri WITHOUT `test`; dev edges add
it; upstream `test = []` is empty; workspace `resolver = "2"`
explicit; Cargo.lock unchanged is the corroborating byte-level
signal). The not-exact-pinning justification is correct cargo
semantics (unification would silently exact-pin the main dep too).
Diff hygiene: no process spawns, no network, no port 1420, no
`unsafe`, no secrets, no bypass flags; headless throughout
(MockRuntime end to end — even my granted-dialog mutation died at
argument parsing, no dialog reachable).

Non-blocking observations: (1) `PickInFlight` is not bound to the
WatchState instance that issued it — a guard from one state would
"serialize" another; unreachable in production (single managed state),
misuse requires constructing two states — note, not a defect.
(2) frontmatter `status:` still said `building` at verification start —
the T-018 builder pattern sets `verifying` at finish and this builder
did not; left untouched per house pattern (neither the T-018 nor T-011
verifier commit moves status), integrator reconciles.

Probe hygiene: every mutation and probe reverted (capabilities/ ×3,
extra.json removed, two verifier probe tests in acl_pin.rs, the
docs_watch broad-lock mutation); `git status` clean before this
commit; suites re-run green after each revert. No servers, no ports
bound or contacted, no screen control, no real model calls.

Suggestions filed: T-021-s3 (pin the panic-path latch release as a
permanent test — today it is structural via Drop; a refactor to manual
latch stores would lose it silently).
