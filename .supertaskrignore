# supertaskr-index walk excludes (gitignore syntax, T-009 plan §3).
# docs/ is the brain, not the codebase; fixture trees are deliberately
# broken indexer inputs, not map content.
docs/
app/src-tauri/crates/supertaskr-index/tests/fixtures/
# Dev tooling that drives the app from outside (T-020: the E2E lane) —
# config-excluded like docs/, never registry territory.
tools/
# Nested working trees the harness may park INSIDE the repo (the
# marketing session's .claude/worktrees/ entry indexed a full second
# copy of the tree: 391 files, every symbol degraded away — the
# T-153-s3 class with a new member, 2026-08-30). Never walk them.
# ROOT-ANCHORED on purpose: an unanchored `bin/` ate
# app/src-tauri/src/bin/fake_agent.rs and the index's own binary on
# its first regen — gitignore patterns without a leading slash match
# at every depth.
/.claude/
/bin/
