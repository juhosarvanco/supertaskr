# Attack set — T-902 (FIXTURE, not a real bench return)

Synthetic, and DIFFERENT REAL CONTENT rather than a placeholder. The
mismatch fixture card cites THIS file's digest against the T-901 file, so
the wrong answer is a well-formed sha256 of something that exists — never
an empty string and never `deadbeef`, both of which a truthiness check
refuses for free. MF-09's MISMATCH row is the same lesson.

1. A different set entirely.
