# Attack set — T-901 (FIXTURE, not a real bench return)

Synthetic. This file exists so the ACCEPT path of the verdict-digest
checker is checkable FROM THE TREE ALONE, with no scratchpad and no
machine-scoped path. Its bytes are pinned by the digest the fixture card
beside it cites, so an edit here reds MF-10 by name.

NO REAL ATTACK SET IS COMMITTED ANYWHERE IN THIS TREE, and that is a
decision rather than an omission: `method/roles/orchestrator.md` 5c says
THE ATTACK SET NEVER REACHES THE EXECUTOR, and where a real set should
live after its verdict is the card's own open design question — the
architect's to answer, not a lane's.

1. The criterion is satisfied by a constant that cannot vary.
2. The control is decided by the same arrangement as its subject.
3. The count is quoted from an earlier ref rather than re-derived.
