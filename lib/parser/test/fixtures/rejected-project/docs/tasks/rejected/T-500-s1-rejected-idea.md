---
title: Rejected suggestion (must stay out of the model)
status: rejected
suggested_by: executor codex @T-501
---

A triaged-away suggestion in the v0.1.4 encoding: moved under
docs/tasks/rejected/ keeping status: rejected. Deliberately id-less and
placement-less — if collection ever went recursive, this file would BOTH
add a phantom record AND raise missing-field issues, so the regression
fails loudly in two independent ways.

Triage 2026-08-15 (architect): REJECTED — example one-line reasoning.
