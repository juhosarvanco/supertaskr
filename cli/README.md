# supertaskr — the published command's root

This directory is the root of the `supertaskr` npm package: the command a project installs, importing the scripts under tools/e2e/ rather than re-hosting them. The directory was created as the bootstrap for T-244-s1 on the owner's ruling of 2026-09-13; the package manifest, the bin and every shared module are that card's lane's to write, and nothing here is published by anyone but the owner (T-266).
